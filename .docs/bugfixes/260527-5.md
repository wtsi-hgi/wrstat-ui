- [x] Production `t283_imaging` summarise can fail after diagnostics show a single raw DGUTA ClickHouse flush of `block_rows=100000` spending over 10 minutes then resetting the TCP connection (`connection reset by peer`); reduce or decouple raw DGUTA flush block size so large directory-heavy imports do not rely on 100k-row native DGUTA blocks, while preserving row counts, snapshot cleanup semantics, and the previous projection memory improvement.
  - Added an independent raw DGUTA batch cap so a 100k import batch now flushes DGUTA rows in 10k native blocks, while children imports keep the configured batch size.
  - Added a regression test that appends more than the raw DGUTA cap under `SetBatchSize(100_000)` and proves the flushed block never reaches 100k rows.
  - Kept the existing projection batch cap intact so the previous summarise memory reduction is preserved.

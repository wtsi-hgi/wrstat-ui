- [ ] A summarise job for `/nfs/wrstat/lustre/20260617-200026_／lustre／scratch124/stats.gz` on mount `/lustre/scratch124/` still fails with `non-contiguous directory input` after the unordered-stats retry work. The attached log shows the retry parse progressing to 11,000,000 records with low RSS, then failing in `current_phase=parse_failed` with `err="non-contiguous directory input"`.
  - Investigation note: the exact production `stats.gz`, output spool, and `/lustre/scratch124/` mount are not readable in this environment, so the failing stream could not be inspected directly. Current HEAD includes `ce31a4b`, which already adds regression coverage for the identified remaining class where retry sorting by raw full path can order prefix-sharing siblings such as `project.v2/` before `project/` and later trip `summary.ErrNonContiguousInput`; current code now sorts parsed records, explicit directory rows, and synthetic parent rows by path components. I read the retry sorter, `stats.NewStatsParser`, `summary.Summariser`, `summary.DirIDAllocator`, and `summary/dirguta`, ran the existing targeted regressions, and also used a temporary scratch124-shaped probe with prefix-sharing siblings, missing parents, duplicate directory boundaries, and shuffled rows through the real sorter plus summariser/allocator; no current-code `non-contiguous directory input` was reproduced. To prove another bug remains, we need read-only access to the exact production `stats.gz` or at least a small ordered excerpt around the first directory path that is re-entered after its subtree was closed, plus confirmation of the binary/commit that produced the attached 2026-06-18 log.

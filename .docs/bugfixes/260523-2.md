- [x] Need an enforced minimum of --min_mem, defaulting to 8GB, and 30mins for summary commands submitted by watch.
  - Added regression coverage for watch-created summarise jobs proving the 8GB RAM default, 30 minute runtime floor, clamping of lower `--min_mem` values, preservation of higher `--min_mem`, and per-mount requirements groups.
  - Changed watch job creation to submit explicit wr requirements with `Override == 1`, so wr treats the submitted RAM/time as floors while still raising them from learned per-mount requirements when higher values exist.
  - Updated watch command expectations and CLI help text for the new default floors.

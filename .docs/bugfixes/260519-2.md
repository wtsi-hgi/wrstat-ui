- [x] `wrstat-ui summarise -d <output> ...` fails in ClickHouse mode when `<output>` is the dot-prefixed staging directory that `watch` creates, such as `.20260517-200015_／lustre／scratch123`; production watch commands use this staging form before `mv`, so ClickHouse summarise cannot currently run from watch.
  - Updated `internal/mountpath/mountpath.go` so mount path derivation accepts the single leading dot used by watch staging directories.
  - Added a regression in `internal/mountpath/mountpath_test.go` proving `.20260517-200015_／lustre／scratch123` resolves to `/lustre/scratch123/`.

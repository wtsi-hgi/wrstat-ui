- [x] Despite `.docs/bugfixes/260603-1.md` and `.docs/bugfixes/260604-1`, t283_imaging is still failing to summarise. Recent failed attempt log tails show a long run stuck/progressing at 320,000,000 records after 6h57m in `current_phase=wrstat_children_insert` with heap_alloc_mb=33628, heap_sys_mb=37494, rss_mb=35744, and two later attempts failing with `clickhouse: failed to flush basedirs batches: clickhouse: failed to send basedirs group usage batch: context deadline exceeded` repeated four times. Try again to do a complete and thoughtful fix; do not be narrowly focused on the exact error seen in the logs. Consider if there is something bad in t283_imaging vs scratch127 beyond "more directories", perhaps further in to the dataset than normally tested with. You must not regress summarise or query speed or significantly worsen summarise memory usage, so take current and after clickhouse-perf measurements to prove this. Learn from past mistakes and do everything possible to prevent this being an endless cycle of "you think it's fixed, but then there's another timeout issue or other problem".
  - Root cause: basedirs batches could still stay open across a change from one basedirs table to another. A partial group-usage insert, for example, could be prepared and then left idle while user usage, subdir, history, or finalise work progressed; t283's long directory-heavy shape made that stale prepared insert vulnerable to the ClickHouse receive timeout when it was finally flushed.
  - Files: `clickhouse/basedirs_store.go`, `clickhouse/basedirs_store_test.go`.
  - Approach: added basedirs write-phase tracking so any pending basedirs batches flush before switching to another basedirs write phase, including usage, subdirs, history, and finalise paths. Added an injectable batch clock so stale-batch regressions are deterministic in tests.
  - Coverage: added a regression proving a pending group-usage batch is sent before user-usage writing starts; prior side-query/finalise ordering regressions remain intact.
  - Perf proof saved under `.tmp/agent/260605-1-baseline/` and `.tmp/agent/260605-1-after/`: sampled 2M-row imports preserved row counts. t283 changed from 80.87s / 706.82 MiB RSS to 78.57s / 706.88 MiB RSS; scratch127 changed from 15.40s / 658.59 MiB RSS to 15.07s / 674.22 MiB RSS. Query timings stayed stable or improved in the recorded operations; t283 examples include `tree_new` 13.33ms to 11.03ms, `tree_where` 15ms to 14ms, `listdir` 8ms to 8ms, and glob operations 9/10ms to 8/9ms.
  - Dataset-shape check: artifacts record t283 as highly directory-heavy through 14M scanned rows, commonly about 55-67% directories per 1M window, while scratch127's first 5M rows were about 6.6% directories with deeper/longer paths.
  - Review: independent reviewer returned PASS and verified the regression would fail without the fix, prior regressions were preserved, and the perf artifacts support no material speed or memory regression on the sampled data.
  - Gates: `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./clickhouse ./cmd ./internal/chperf`; `timeout 600s make testnonpm`; `timeout 300s make lintnonpm`; `timeout 120s go mod tidy -diff`; `timeout 120s git diff --check`.

- [x] Production summarise publish can reach `spool_load_wrstat_files` and fail against an existing pre-overhaul ClickHouse table with `clickhouse: failed to prepare wrstat_files batch: code: 16, message: No such column dir_id in table prod.wrstat_files`, after `summarise parse complete` for `/nfs/wrstat/lustre/20260617-200026_／lustre／scratch123/stats.gz`.
  - Root cause: schema bootstrap used final `CREATE TABLE IF NOT EXISTS` DDL, which leaves an already-existing `wrstat_files` table on the pre-overhaul layout. Because schema version stayed at v1, the loader could pass bootstrap and only fail later when preparing the `wrstat_files` insert that includes `dir_id`.
  - Fix: bootstrap now performs an idempotent `ALTER TABLE wrstat_files ADD COLUMN IF NOT EXISTS dir_id UInt32 CODEC(Delta, LZ4) AFTER snapshot_id` under the existing schema bootstrap lock, with an actionable error if ClickHouse refuses the repair. Existing final-schema tables are unchanged; pre-overhaul tables become able to store real `dir_id` writes before spool replay starts.
  - Regression coverage: added a ClickHouse-backed spool publish test that seeds a current-version database with a pre-overhaul `wrstat_files` table missing `dir_id`, verifies the missing column, loads a schema3 spool containing a real file row, and proves the row lands with the expected `dir_id` and the snapshot publishes active.
  - Gates: `cleanorder -min-diff clickhouse/schema.go clickhouse/summarise_spool_loader_test.go`; `golangci-lint run --fix`; `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./clickhouse ./cmd -run 'TestClickHouseSummariseSpoolLoader|TestSummariseClickHouseSpoolRows|TestSummariseStatsSort' -v`; `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./clickhouse ./cmd ./internal/chperf`; `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./clickhouse ./cmd ./internal/chperf -v`; `timeout 120s git diff --check`; `timeout 300s make lintnonpm`; `timeout 900s make testnonpm` passed.
- [ ] In the same build that included the unordered-stats retry fix from `87985c7`, production logs still include current-code `non-contiguous directory input` parse failures for `/nfs/wrstat/nfs/20260615-170002_／warehouse／team205_wh01/stats.gz` and `/nfs/wrstat/nfs/20260616-170003_／nfs／cancer_scratch/stats.gz`; fix the remaining raw-ordering shape and keep unordered-data regression tests in place.

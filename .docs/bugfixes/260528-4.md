- [x] Long-running ClickHouse summarise imports can still fail after hours of successful bounded sends when a fresh batch preparation uses the normal query timeout, as in the production `scratch126` run that reached 145,000,000 records and then failed with `clickhouse: failed to prepare files batch: context deadline exceeded`; audit and fix the whole long-running import context/prepared-batch lifecycle so repeated ClickHouse batch prepare/send/reset/cleanup operations remain robust for very large mounts without regressing bounded memory, retry safety, or the previous holistic send/cleanup fixes.
  - Centralised prepared-batch import context handling in `prepareBatchWithRelease`: ClickHouse batches now detach from normal query cancellation/deadlines because clickhouse-go stores the prepare context and reuses it during later `Send()`.
  - Removed local one-off `WithoutCancel` wrapping from DGUTA initial prepare, file ingest prepare, and basedirs reset/reprepare paths so DGUTA, children, projection, files, and basedirs all rely on the same import-batch helper while existing cleanup/partition drops keep cleanup-timeout semantics.
  - Added `clickhouse/import_context_test.go` regressions for expired normal query contexts across prepared batch send lifecycle, reprepare after send, all long-running import prepared-batch tables, the file-ingest fresh files batch case, and cleanup with an expired parent context.
  - Verified with `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./clickhouse ./cmd ./internal/chperf`, `timeout 600s make testnonpm`, `timeout 300s make lintnonpm`, `timeout 120s go mod tidy -diff`, and `timeout 120s git diff --check`.

- [x] 1. Differentially test and repair disk-backed hardlink aggregation
  - Added forced-disk differential fixtures for same-directory, sibling, nested, classification-changing, timestamp, size, and all age-bucket hardlink cases, comparing every emitted field with memory mode and independently rejecting stale aggregates.
  - Kept hardlinks in the existing per-node inode maps until final materialisation; SQLite now aggregates and rolls up ordinary rows only, while exact inode summaries merge upward separately in both disk and memory paths.
  - Removed lossy aggregate subtraction and stale zero-count keys. Focused tests, `make lintnonpm`, `make testnonpm`, and independent review all passed.
- [x] 2. Replace the compressed-size routing heuristic with an exact shape probe
  - Removed the 32 MiB shortcut and exact-probed every reopenable input, retaining the existing fast path for fully contiguous input and routing the first boundary violation directly to `dirbuild`.
  - Recorded the exact source row and path depth for the first violation, including inputs that synthesize parent records; non-contiguous stdin now fails explicitly instead of attempting an impossible second pass.
  - Added large-contiguous, early-unordered, synthesized-parent, and stdin regressions. Focused tests, `make lintnonpm`, `make testnonpm`, and fresh independent review passed.
- [x] 3. Make SQLite aggregation sequential and write-combining where possible
  - Added a fixed 4 MiB write-behind accumulator over the full compact-row key, combining repeated facts and flushing in deterministic sorted order without growing with directory count.
  - Replaced duplicate child reads with one ordered select that emits compact rows, queues the same ordinary rows into the parent, and deletes the child while exact hardlinks continue to roll up separately.
  - Added observable combining/statement/database/phase metrics, kernel-observed process write-byte deltas, separate pass-2 and roll-up timings, and correct scratch retention on failure/removal after canonical success. Focused tests, `make lintnonpm`, `make testnonpm`, repeated memory tests, and fresh review passed.
- [x] 4. Add phase and shape telemetry that works during a multi-hour run
  - Added live build, roll-up, table, and derived-stage phases with rows, directories/implied directories, depth, RSS, SQLite/spool bytes, rates, elapsed time, batches, optional server bytes/parts, and the persisted checkpoint.
  - Added fixed-capacity mount-relative depth/heavy-prefix shape state, avoiding all recorder-free work and per-row snapshot allocation; periodic parse/probe logs and blocking derived-stage heartbeats now advance throughout long work.
  - Preserved completion-recorder and publication semantics, made unavailable metrics honest, and added boundedness, mount-relative, retry/readiness, heartbeat, cancellation, race, and peak-memory regressions. Full lint/test gates and final independent review passed.
- [x] 5. Load and validate cheap ClickHouse tables before the huge file table
  - Reordered publication to load directories, facts, filters, derived child filters, validation, and the amplification gate before files; file import and verification now precede readiness/finalisation.
  - Added exact-order, derived-failure/no-files, and readiness-withheld-on-file-verification regressions while preserving the existing coarse checkpoint for item 6.
  - Focused integration tests, `make lintnonpm`, `make testnonpm`, and independent review passed.
- [x] 6. Checkpoint publication per table, not only after all spool tables
  - Added durable v2 per-table and per-derived-operation checkpoints containing verified row counts, written atomically with file and directory sync.
  - Retries server-revalidate every persisted checkpoint, skip exact matches, and durably clear/reset/reload only a stale or incomplete stage; v1 state supplies provenance only and is reverified before promotion.
  - Added crash-window, scoped-count, selective-reset, derived-operation, migration, corruption/version, mixed-partition, readiness, and retry coverage. Full gates and final independent review passed.
- [x] 7. Put an immediate global concurrency cap on summarise jobs
  - Added `watch --summarise_concurrency` with safe default one and validation, assigning every scheduled summarise job to the shared `wrstat-ui-clickhouse-import:<count>` limit group.
  - Preserved the legacy `watch.Watch` API and added a configurable options entry point; scheduled execution carries a non-secret job-environment marker while manual and copied commands warn that they are outside the guard.
  - Added public-API, default/custom/invalid, multi-job, stored-command, help, warning, and integration coverage. Full gates and independent review passed.
- [ ] 8. Use byte budgets and server pressure guardrails for ClickHouse inserts
- [x] `make lintnonpm` fails after item 2 because `summariseSpoolProbeContiguity` is 31 lines, exceeding the `funlen` limit of 30.
  - Evidence: independent review reported `cmd/summarise_spool.go:985` while focused tests, `make testnonpm`, and `git diff --check` passed.
  - Removed one behavior-neutral blank line to meet the limit without a suppression or source-shape test; the lint and full Go test gates then passed in implementation and fresh review.
- [x] Item 3 reports synthetic SQL argument widths as `sqlite_write_bytes`, which does not measure SQLite/NFS write I/O or B-tree/page amplification.
  - Evidence: independent review found fixed 216-byte upsert and 8-byte delete increments feeding the pass-2/roll-up byte metrics, while tests asserted only that the result was nonzero.
  - Replaced the synthetic counter with explicitly named Linux `/proc/self/io` process-write deltas, availability/source metadata, controlled exact-delta tests, and safe unavailable-platform behavior.
- [x] Item 3's fixed-memory test is self-referential and does not prove accumulator heap use remains independent of directory scale.
  - Evidence: independent review found the test checked a counter derived from the same constant used for flushing without varying directory scale or measuring heap growth.
  - Added a GC/`runtime.ReadMemStats` regression comparing 20,000 with 200,000 distinct directory IDs; retained heap stayed near 2 MiB with small scale deltas over repeated review runs.
- [x] Item 4 phase-start telemetry invokes the established completion recorder with zero duration, breaking checkpoint, retry, cancellation, and readiness callback semantics.
  - Evidence: independent review reproduced three `cmd` test failures with duplicate retry phases and premature readiness callbacks.
  - Moved starts to a separate live channel and restored completion-only callbacks; exact retry, cancellation, and readiness regressions pass.
- [x] Item 4 clones and sorts the bounded shape report on every directory-scan row, even without a progress recorder.
  - Evidence: independent review traced per-row `setShape` calls to 65-bin cloning and heavy-prefix allocation/sorting despite emission occurring only every 100,000 rows.
  - Shape snapshots now occur only at emission cadence, shape state is not allocated without a recorder, and allocation/callback regressions cover recorder-free paths.
- [x] Item 4 heavy prefixes are rooted at filesystem `/` instead of the selected mount, collapsing normal mount telemetry to the mount's own first component.
  - Evidence: integration telemetry for `/mnt/test/` reported only `mnt/` despite distinct descendant prefixes; tests covered only mount `/`.
  - Both dirbuild and contiguity-probe prefixes are now relative to the selected mount, with exact non-root descendant coverage.
- [x] Item 4 periodic diagnostics omit the new shape/storage/rate/phase-elapsed fields on long contiguity-probe and ordinary parse paths.
  - Evidence: independent review found the fields appear only after later state transitions, and ClickHouse batch logs report zero phase elapsed.
  - Periodic parse/probe diagnostics now include all live fields and ClickHouse phase elapsed advances with subsecond precision.
- [x] Item 4 marks ClickHouse byte telemetry available when a server progress packet reports zero `WroteBytes` for a nonempty insert.
  - Evidence: integration output reported `clickhouse_rows_sent=155 clickhouse_bytes_sent=0` as available; tests bypassed the real progress wiring.
  - Zero server `WroteBytes` for nonempty inserts remains unavailable, verified through the real loader/progress integration.
- [x] Item 4's bounded heavy-prefix implementation fails the existing peak-memory regression because map churn creates excessive transient allocation.
  - Evidence: the first `make testnonpm` feedback run exposed the failure; the implementor replaced the map-backed counters with fixed arrays before rerunning the full gate.
  - Replaced map churn with fixed-capacity arrays and preserved bounded Misra–Gries behavior.
- [x] Item 4 leaves the A4.7 dirbuild peak-memory regression order/heap-state sensitive in the full test suite.
  - Evidence: fresh review's `make testnonpm` observed 160,784,384 bytes against the 158,331,648-byte budget, while isolated and repeated runs usually passed.
  - Made measurement deterministic under the production dirbuild GC policy without changing the budget; repeated A4 and full-suite review runs pass.
- [x] Item 4 emits telemetry only before and after a blocking derived ClickHouse query, leaving a multi-hour derived stage without advancing breadcrumbs.
  - Evidence: fresh review traced `deriveChildFilterAll` as one blocking operation with live telemetry emitted only around the call.
  - Added serialized 30-second heartbeats around blocking derived stages with cancellation and synchronous ticker shutdown, covered by deterministic and race tests.
- [x] Disabling recorder-free item 4 telemetry exposes nil dereferences in dirbuild memory and pass-2 paths.
  - Evidence: the feedback implementation's focused memory/pass-2 tests failed after telemetry became optional; nil-safe row accounting was added before full reruns.
  - Made optional telemetry row access nil-safe; focused, repeated, full-suite, and race-enabled review gates pass.
- [x] Item 6 completed `tables_loaded` retries bypass server-count revalidation and can skip stale or missing rows.
  - Evidence: independent review found the loader returns immediately for syntactically valid checkpoints; existing stale coverage exercised only an incomplete pre-`tables_loaded` run.
  - Removed the early bypass; completed retries now exact-revalidate all persisted stages and selectively replay stale/missing work.
- [x] Item 6 v1 migration fabricates verified per-table and derived checkpoints from the coarse `tables_loaded` marker without querying ClickHouse.
  - Evidence: v1 carried no per-table evidence, especially for basedirs history, but the new migration test asserted that unsafe promotion.
  - V1 now creates no verification evidence and queries every stage, including exact basedirs-history keys, before v2 promotion.
- [x] Item 6 treats unsupported publish-state versions as benign identity mismatches and may destructively reset target partitions.
  - Evidence: independent review traced incompatible state through snapshot preparation instead of a fail-closed error before database mutation.
  - Unsupported versions and corrupt JSON fail explicitly before database access or mutation.
- [x] Item 6's fault-injection spy erases every active-set partition during old-set cleanup, masking selective peer reuse.
  - Evidence: feedback tests exposed that the spy modeled a targeted active-set drop as a global reset; it was corrected to remove only the addressed active-set ID.
  - Corrected targeted active-set modeling and followed with row-level mixed-batch filtering so verified peers remain observable.
- [x] Item 6's active-set spy still drops or retains an entire mixed-partition batch based only on its first row.
  - Evidence: fresh review found targeted deletion must filter individual batch rows and update stored counts; current tests can still mask peer loss within one batch.
  - The spy now filters each row, adjusts stored counts, and proves exact peer survival with target-first and peer-first batches.
- [x] `make lintnonpm` reports unsafe `args[1]` access in the item 6 loader spy.
  - Evidence: the mixed-partition feedback cycle exposed `G602` in `clickhouse/summarise_spool_loader_test.go` while running the mandatory lint gate.
  - Added explicit argument-count validation; lint, repeated loader/retry tests, the full suite, and final review pass.
- [x] `make lintnonpm` reports whitespace formatting in item 7's multiline watch command-shape assertion.
  - Evidence: the first item 7 lint gate failed; the assertion was rewritten as a named boolean before final gate reruns.
  - Rewrote the assertion and completed lint/full-suite verification.
- [x] Item 7 breaks the exported `watch.Watch` Go API by adding a required positional concurrency argument.
  - Evidence: independent review found existing external callers would fail to compile; the original signature must retain default concurrency one via a new options/configurable entry point.
  - Restored the exact legacy signature/default behavior and introduced `watch.WithOptions` for configurable callers.
- [x] Item 7 trusts a user-supplied hidden `--scheduler-guarded` flag to suppress the manual concurrency warning.
  - Evidence: copying a stored scheduled command and running it manually suppresses the warning even though it is outside the scheduler guard.
  - Removed the CLI flag and command argument; a dedicated exact-value `Job.EnvAddOverride` marker keeps scheduled runs silent while copied/manual commands warn.
- [x] Item 7 feedback changes fail lint for copied lock values, stuttered API naming, long lines, and an unannotated test shell invocation.
  - Evidence: the first feedback lint run reported `copylocks`, API-stutter, `lll`, and `gosec` findings before the final gate rerun.
  - Corrected the new code/test quality findings without production suppressions; final lint and test gates pass.
- [ ] `make lintnonpm` reports whitespace formatting in item 8's pressure-threshold validation condition.
  - Evidence: the first item 8 lint gate failed; validation was rewritten as a fixed-array loop without suppression before rerunning gates.
- [ ] Item 8 checks the byte target only after appending a row, allowing ordinary multi-row batches to exceed the configured budget.
  - Evidence: independent review found two 3-byte rows with a 5-byte target are sent as one 6-byte batch; only a single oversized row should exceed the target.
- [ ] Item 8 merge pressure counts all server merges instead of merges for the table currently being loaded.
  - Evidence: `system.parts` is scoped by database/table but `system.merges` is not, so unrelated server activity can indefinitely pause this load.
- [ ] `make lintnonpm` reports whitespace formatting in an item 8 feedback regression test.
  - Evidence: the first feedback lint gate failed after the boundary/query-scope tests were added; formatting was corrected before full reruns.
- [x] Concurrent ClickHouse-backed test runs can collide on a supposedly free local port and fail with ClickHouse exit 210/address in use.
  - Evidence: an item 8 full-suite feedback run overlapped a previously lost run and failed; isolated ClickHouse tests and a clean full rerun passed.
  - Added a shared loopback-only launcher with three fresh-port/directory attempts, exact bind-collision classification, fail-fast for other errors, readiness verification, and complete failed-attempt cleanup.
- [x] The root `main_test.go` ClickHouse integration harness still uses the vulnerable pick-close-start port sequence.
  - Evidence: independent review found active `setupClickHouseCLIEnv` callers remained on the legacy launcher after the package harness migrations.
  - Migrated root, clickhouse, cmd, and server harnesses to the shared launcher while preserving local/external DSN behavior.
- [x] The shared ClickHouse test launcher lacks behavioral proof for concurrent launches and successful-process `Stop` cleanup.
  - Evidence: independent review found occupied/persistent/non-bind failure coverage but no direct concurrent-launch or no-orphan-after-stop regression.
  - Added concurrent real-server, bounded retry, fail-fast, idempotent stop, waited termination, closed-listener, and no-orphan regressions; stress, race, lint, and full test gates passed.
- [ ] Item 8 swallows parent cancellation when the real pressure query returns an all-unavailable sample.
  - Evidence: independent review found the loader then prepares a detached next-batch context, so ingestion can continue after cancellation; existing tests covered only cancellation during poll waiting.
- [ ] Item 8's first cancellation fix breaks established detached import-context semantics by guarding every batch preparation.
  - Evidence: the first full test rerun failed; the fix must be limited to the pressure-triggered transition before the next detached batch.
- [x] `TestNewClientBootstrapLockWaitDoesNotUseQueryTimeout` flakes when its cross-process helper times out creating the temporary ClickHouse database.
  - Evidence: the second item 8 cancellation full-suite rerun failed only this unrelated harness test after focused pressure/import-context/race/lint gates passed.
  - Pre-created the empty database under the independent 15-second coordination context before taking the schema lock, leaving helper `NewClient`, the 100ms query timeout, the >400ms lock wait, and singleton schema-version assertions intact.
  - Combined stress, race stress, lint, full tests, and independent review passed.

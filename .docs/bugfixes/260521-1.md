- [x] When `wrstat-ui watch` submits `wrstat-ui summarise` jobs to wr, it is using “wrstat-ui-summarise” as the requirements group, so wr isn’t learning memory/time usage on a per-mount basis. Make watch include the mount path (eg. “／lustre／scratch127” in this case, excluding the date&time part of the discovered directory name) in the requirements group, so it’s possible for wr to learn higher memory and time usage of certain mounts.
  - Updated watch job creation so the wr requirements group is `wrstat-ui-summarise-<dataset key>`, excluding the discovered directory timestamp/version prefix.
  - Added package-level regression coverage for `20260517-200015_／lustre／scratch127` and updated watch CLI expectations.
  - Verified with focused watch tests plus `make testnonpm` and `make lintnonpm` in the implementor and reviewer cycles.
- [ ] Investigate if there is something special about the scratch127 stats.gz file compared to the others that triggers the higher and spikey memory usage and also figure out what is the cause of the relative slowness. Then find a way to fix both. Prove both memory and time issues are solved with before and after runs using a sufficiently large subset of the file, or worst case a full run on the entire file.

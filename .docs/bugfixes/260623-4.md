- [x] They're still getting killed. Try a better fix. Production `summarise` for `/nfs/wrstat/lustre/20260622-200025_／lustre／scratch120/stats.gz` starts at `2026-06-23T14:23:31+0100`, then at `2026-06-23T14:24:42+0100` logs `summarise signal` in `current_phase=parse` with `records=0`, `elapsed=1m11s`, `heap_alloc_mb=8494`, `heap_sys_mb=9151`, `rss_mb=9386`, `gc_count=49`, `mount_path="/lustre/scratch120/"`, `query_timeout=30s`, and `signal=SIGINT`.
  - Regression context: commit `0c5ba54` routed large compressed stats files directly to dirbuild, but this log shows the same mount still exceeds the 8GB job limit. The next fix must reduce dirbuild's own parse/build memory rather than only skipping the fast parser.
  - Root cause: dirbuild still built an overly heavy directory index before any pass-2 file rows could be reported. Every `dirNode` preallocated an empty GUTA map and hardlink map, and the retained index kept both `path -> dir_id` and `dir_id -> node` maps even though parent traversal already follows directory structure.
  - Fix: dirbuild now lazily allocates per-node GUTA stores and hardlink maps only when pass 2 actually adds a summary or sees a hardlink, and each node keeps a parent pointer so rollup/synthetic directory replay no longer needs a retained `dir_id -> node` map. The path builder also stopped keeping a reverse directory key map while assigning IDs.
  - Regression coverage: added an A4.0 directory-only index memory regression with 250,000 directory rows and no file rows. Before the fix, this synthetic shape measured about 184 MB peak heap growth; after the fix it measured about 139 MB locally and must stay below a directory-scaled budget. Existing A2/A4 dirbuild and A3 cmd spool tests continue to cover unordered/non-contiguous correctness, hardlinks, implied directories, and the small contiguous fast path.
  - Verification: `CGO_ENABLED=1 go test -tags netgo --count 1 ./summary/dirbuild -v -run 'TestBuildA4BoundedMemoryAndBytesWritten|TestBuild'`; `CGO_ENABLED=1 go test -tags netgo --count 1 ./cmd -v -run 'TestSummariseClickHouseSpoolRows|TestSummariseClickHouseSpoolB2Retry'`; `CGO_ENABLED=1 go test -tags netgo --count 1 ./summary/dirbuild`; `CGO_ENABLED=1 go test -tags netgo --count 1 ./cmd`; `cleanorder -min-diff summary/dirbuild/dirbuild.go summary/dirbuild/dirbuild_test.go`; `golangci-lint run --fix`; `make lintnonpm`; `make testnonpm`; `git diff --check`.

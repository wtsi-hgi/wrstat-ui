- [x] Large directory-heavy ClickHouse summarise jobs such as `t283_imaging` can still require too much memory after direct projection writes; reduce peak RSS on realistic `t283` prefixes while preserving imported row counts, snapshot correctness, and query-speed projections, and report any runtime tradeoff honestly.
  - Added a separate bounded ClickHouse projection batch size: raw DGUTA/children imports keep the configured 100k batch, while maintained dir-summary/vector projection rows default to a 10k cap and can still flush lower when the configured import batch is lower.
  - Added regression coverage proving large raw import batches cap projection batches by default, and that projection batches can flush at a lower size while raw DGUTA/children batches continue to use the larger import batch.
  - Measured current HEAD on the 1M `t283_imaging` prefix before the fix: 42.79s wall, 3,126,912 KB max RSS, rows preserved at files=1,000,000, DGUTA=4,516,772, children=230,309, dir_summary=2,946,425, dir_vector=230,310, summary_sets=1, active snapshots=1.
  - Measured the same 1M prefix after the fix: 39.59s wall, 1,101,412 KB max RSS, identical row counts and active snapshot; projection phase changed from 25.56s to 22.62s.
  - Ran one 2M after-fix proof: 2:23.83 wall, 1,178,644 KB max RSS, rows files=2,000,000, DGUTA=18,044,757, children=818,478, dir_summary=11,768,795, dir_vector=818,479, summary_sets=1, active snapshots=1. Compared with the prior 2M evidence of 3,227,056 KB and 143.154s, this cuts peak RSS by about 63% with roughly neutral runtime. The full 5.7GB compressed production file was not run.

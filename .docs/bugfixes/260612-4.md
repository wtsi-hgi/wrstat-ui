- [x] A lot of summarise commands are working now, but some still fail. Here's an example where a mount fails on 2 attempts in a row. Use the the bugfix skill to fix.
  - Files: `clickhouse/dguta_writer.go`, `clickhouse/summarise_spool_loader.go`, `clickhouse/summarise_spool_loader_test.go`.
  - Gave ClickHouse close/publish phases fresh close-time contexts and moved active virtual materialisation onto the cleanup-class timeout so zero-record retries do not inherit the normal 30s query deadline during global active-virtual generation.
  - Changed retry basedirs-history cleanup to wait for local mutation visibility with `mutations_sync = 1`, avoiding cross-replica waits while still making exact-key retry cleanup visible before reinserting rows.
  - Added regressions proving active-virtual publish uses the generated cleanup deadline, long staging does not leave a stale switch context, and retry basedirs-history cleanup uses local mutation sync.
  - Reviewer returned PASS. Focused ClickHouse regressions, `make test`, `make lint`, and `git diff --check` passed.

- [x] ClickHouse summarise retries still fail at scale with `clickhouse: failed to clean active snapshot mount row: mount_path=... snapshot_id=... is still active; latest_active_switched_at=... newer than cleanup_baseline=...`, including cases where the printed timestamps appear identical. Take a holistic view of the active-snapshot lifecycle so cleanup, retry, concurrent publishing, and ClickHouse timestamp precision cannot strand deterministic snapshots as active or cause most scheduled jobs to fail.
  - Fixed cleanup guard and repair SQL to compare `DateTime64(3)` `switched_at` values against explicit millisecond epoch baselines via `fromUnixTimestamp64Milli(?)`, avoiding driver/server `time.Time` precision loss.
  - Added regressions for same-millisecond active rows not being treated as newer than the cleanup baseline and for no-effect tombstone repair using millisecond-precise cleanup baselines.
  - Preserved concurrent-publish protection: only active same-SID rows with a genuinely later millisecond `switched_at` still block partition drops.

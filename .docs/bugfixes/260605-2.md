- [x] Despite `.docs/bugfixes/260603-1.md` ,`.docs/bugfixes/260604-1` and `.docs/bugfixes/260605-1.md`, t283_imaging is still failing to summarise. You need to recognise that you are utterly incapable of fixing these issues competely as previously requested. You will need to come up with a radical new approach that abandons the current method entirely to completely remove all possibility of these deadline and timeout and slowness issues. We also return to the fact that this is far too slow, with the bolt method managing to complete successfully in much less time. So clearly this is possible. I think earlier attempts in bugfixes long ago don’t just do everything in memory on the go side because it uses too much memory. But instead of spooling to clickhouse to limit memory usage, why not append to temp files (in the dot output location, not /tmp), and then at the end do very safe and reliable clickhouse interactions quickly? Also think of at least 4 other completely different ideas that could work. Get subagents to systematically try the different ideas with temp code and measure to see if any is signficantly better than the others in terms of time, memory usage and possibility the clickhouse interaction can go wrong. Pick the best and use the bugfix skill workflow to implement that. You must not regress summarise or query speed or significantly worsen summarise memory usage, so take current and after clickhouse-perf measurements to prove this.
  - Additional requirement: regardless of the method picked, if there is a natural point where substantial work is done and then there is a risky bit that could fail, retries of the failed summarise command should not repeat the initial work and should only retry the risky bit.
  - Prototype comparison: five independent approaches were measured on real bounded t283/scratch127 data. Bolt-first export, ClickHouse raw staging, and pure server-side reduction were rejected as primary fixes because they either moved the risk later, kept the checkpoint inside ClickHouse, or required a high-risk semantic rewrite. External sort/reduce was fastest but too broad for this fix. Final-row output-local spooling was selected, with the RowBinary/HTTP prototype informing the future wire-format direction.
  - Approach: `wrstat-ui summarise` now builds a durable ClickHouse spool under the dot output directory before loading ClickHouse. The spool uses atomic partial-to-ready state, a manifest with input/output/config identity, checksums, byte counts, and decoded row-count verification. A retry with a complete matching spool skips stats parsing and reruns only ClickHouse cleanup/load/count/publish. Failed loads keep the old active snapshot untouched, and basedirs history replay deletes the exact manifest-listed keys before reload to avoid duplicates.
  - Files: `internal/chspool/spool.go`, `cmd/summarise_spool.go`, `clickhouse/summarise_spool_loader.go`, `cmd/summarise.go`, `clickhouse/client.go`, `cmd/summarise_spool_test.go`, `clickhouse/summarise_spool_loader_test.go`, `internal/chspool/spool_test.go`, `cmd/summarise_test.go`, `cmd/summarise_diagnostics_test.go`.
  - Regression coverage: completed-spool retry without rereading `stats.gz`, corrupt/mismatched manifest rebuild/reject paths, manifest-only row-count tampering rejection, output-dir identity checks, short query-timeout tolerance during long spool replay, partial unpublished reload idempotence, no duplicate basedirs history, and ClickHouse spool row parity on small fixtures.
  - Perf proof: prototype artifacts under `.tmp/agent/spool-final-rows/`, `.tmp/agent/http-rowbinary-bulk/`, `.tmp/agent/external-sort-reduce/`, `.tmp/agent/ch-staging-server-reduce/`, and `.tmp/agent/bolt-first-export/`; implementation guardrail artifacts under `.tmp/agent/260605-2-baseline/` and `.tmp/agent/260605-2-after/`. After 2M-row `clickhouse-perf` samples reported t283 import `1:19.11` / `690.86 MiB` RSS and scratch127 import `0:15.49` / `655.11 MiB` RSS, with query summaries saved in `.tmp/agent/260605-2-after/query-summary.tsv`.
  - Review: first reviewer found two P1 issues in the initial patch: one replay-wide query-timeout context and manifest row-count/output-dir verification gaps. A second implementor fixed both. The second reviewer returned PASS after verifying fresh per-operation contexts, decoded row-count checks, output-dir identity validation, retry-without-parse behavior, old active snapshot safety, and basedirs history idempotence.
  - Gates: `timeout 300s env CGO_ENABLED=1 go test -tags netgo --count 1 ./internal/chspool -v`; focused `./cmd` and `./clickhouse` spool tests; `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./clickhouse ./cmd ./internal/chperf`; `timeout 600s make testnonpm` (one transient ClickHouse harness port collision, rerun passed); `timeout 300s make lintnonpm`; `timeout 120s go mod tidy -diff`; `timeout 120s git diff --check`.

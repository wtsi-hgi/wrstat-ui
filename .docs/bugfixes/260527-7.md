- [x] Summarise jobs can fail immediately in preflight with `failed to create dguta writer: dial tcp: lookup farm22-wrstat01: i/o timeout`; retry transient ClickHouse connection setup failures inside the process so temporary DNS/dial timeouts do not require scheduler retries.
  - Added bounded, context-responsive retries around central ClickHouse open/ping setup, so `NewDGUTAWriter` and other ClickHouse clients recover from transient DNS/dial/ping timeouts.
  - Added regression tests for DNS and dial timeout errors that wrap `context.DeadlineExceeded`, plus no-retry coverage for bare context deadline/cancellation and non-transient failures.
  - Kept validation, ClickHouse exceptions, and unknown-database bootstrap semantics outside the retry path.

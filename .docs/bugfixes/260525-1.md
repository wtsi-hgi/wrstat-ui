- [x] Active mount dir projection refresh is not just an existing-database backfill: normal daily mount updates can still ask ClickHouse to rebuild `wrstat_dir_summary` and `wrstat_dir_dguta_vector` with expensive `INSERT ... SELECT` queries, leading to multi-hour runs and memory-limit failures. Optimise only for fresh ClickHouse databases/imports: write maintained projection rows during Go summarise/import, remove backwards-compatibility backfill code/comments for old snapshots, and do not preserve the expensive ClickHouse projection rebuild path.
  - Changed normal ClickHouse DGUTA imports so `wrstat_dir_summary`, `wrstat_dir_dguta_vector`, and `wrstat_dir_summary_sets` are written directly from the Go `RecordDGUTA` stream before the snapshot is switched active.
  - Deleted the async mount-dir projection refresh, provider startup tree-summary backfill, and maintenance connection/read-timeout machinery that only existed for old ClickHouse snapshots.
  - Added regression coverage proving normal writer import forbids the expensive `INSERT ... SELECT ... FROM wrstat_dguta` projection rebuild SQL and still produces all/default summaries, file-only summaries, child counts, and arbitrary filtered vector results.

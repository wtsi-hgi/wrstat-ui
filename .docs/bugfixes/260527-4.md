- [x] `wr` does not provide durable stdout/stderr for earlier failed summarise attempts once a retry is running; change watch-submitted summarise commands to redirect stdout/stderr to timestamped per-attempt log files in the staging output directory so diagnostics from past failures remain available.
  - Added watch command regression coverage that executes the generated shell command against a fake summarise binary, proving failed attempts leave combined stdout/stderr logs in staging and a successful retry moves all attempt logs to the final output directory.
  - Changed watch-submitted summarise commands to create `summarise-$(date -u +%Y%m%dT%H%M%SZ)-$$.log` under the hidden staging output directory at run time and redirect summarise stdout/stderr there before `touch && mv`.
  - Switched generated watch command path/executable interpolation to POSIX-safe single-quote shell escaping while preserving intentional `date`/`$$` expansion, and covered `$`, backtick, and `$()` paths so they neither expand nor execute.
  - Updated watch/root command expectations and replaced fragile or tautological rep-group assertions with prefix plus timestamp-shape checks.

- [x] On a new database summarise failed like this straight away.
  - Fixed active virtual partition cleanup after publish so it refuses to drop only the stale active-set partition when that partition is still current, instead of failing whenever another publish advances the global active set.
  - Files: `clickhouse/active_snapshot_cleanup.go`, `clickhouse/dguta_writer_test.go`.
  - Added a regression for the production-shaped close failure where another publish changes `current_active_set_id` after the summarise publish, while the old active virtual set being dropped is already stale.
  - Reviewer returned PASS. Focused ClickHouse tests, `make test`, `make lint`, and `git diff --check` passed.

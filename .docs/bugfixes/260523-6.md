- [x] The committed ClickHouse/Bolt perf query commands cannot isolate Disktree directory-click operations, so slow or failing `where` timings can prevent measuring the frontend directory-click path. Add operation selection to the perf subcommands so directory-click benchmarks can be run independently and compared against Bolt on the same dataset.
  - Added `--ops` to ClickHouse and Bolt perf query commands, including the legacy Bolt interfaces path.
  - Operation selection accepts comma-separated operation names, preserves the built-in operation order, leaves default all-op behavior unchanged, and reports unknown names with available operations.
  - Added behavioural coverage for ClickHouse, Bolt, and legacy Bolt operation filtering.
- [x] ClickHouse-backed Disktree clicks into new non-root directories remain slow compared with Bolt on large real-world subsets. Use the perf subcommands to prove the current gap, then implement storage/query improvements until new-directory Disktree clicks are as good as or better than Bolt on the same dataset.
  - Added maintained per-mount directory summary tables with child counts, schema migrations, import/write refresh hooks, and async startup maintenance for existing active mounts.
  - Used bounded, snapshot-scoped in-memory caches for child rows, maintained summaries, readiness, and DGUTA fallbacks; broad Disktree child checks now prefetch visible child summaries opportunistically without making correctness depend on that prefetch.
  - Added external-table batching for large directory-list lookups and kept UID/GID/FT filtered paths on the existing filtered fallback logic.
  - Added `tree_disktree_endpoint_visible_child_dirs` to the ClickHouse and Bolt perf query suites so future benchmarks can time the web UI path: load a parent, then click visible children.
  - Proved on the same 1.67M-row scratch127 subset that visible-child clicks improved to ClickHouse p50 0.006 ms / p95 0.025 ms for `b7` versus Bolt p50 0.041 ms / p95 0.076 ms; the 1000-child case was ClickHouse p50 0.003 ms / p95 0.014 ms versus Bolt p50 0.025 ms / p95 0.043 ms; a 15,892-child synthetic case stayed web-responsive at ClickHouse p50 16.637 ms / p95 27.520 ms.

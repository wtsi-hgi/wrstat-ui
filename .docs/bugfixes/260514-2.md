- [x] What do you make of the npm-shrinkwrap.json change? It's not something I did on purpose, but something that happened when I ran one of the commands to test or build this app (I note something else added the peer lines and I committed it in b87296d5e83ab42ed33255261a7b7dd541967496). Commit the change or fix something to stop the change happening as appropriate.
  - Restored `server/static/wrstat/npm-shrinkwrap.json` to the committed peer-marker form from `b87296d5e83ab42ed33255261a7b7dd541967496`.
  - Changed frontend Makefile recipes to use `npm ci`, verified it installs successfully and leaves the shrinkwrap unchanged.
- [ ] Then get `make testnonpm` to pass without bypasses.
- [ ] Then get `make lintnonpm` to pass without bypasses.

- [x] ClickHouse active snapshot cleanup can still fail with `clickhouse: failed to clean active snapshot mount row: mount_path=... snapshot_id=... is still active`; cleanup must reliably make the failed deterministic snapshot inactive, and must prove whether this is caused by active-view tie ordering, zero-row tombstone inserts, or concurrent cleanup/publish races.
  - Proved the active view was tie-sensitive: `argMax(..., switched_at)` could choose a failed active row over an inactive tombstone when rows shared the same `switched_at`.
  - Changed `wrstat_mounts_active_v2` to use deterministic tuple ordering so inactive rows win same-timestamp ties while later active publishes still restore activity.
  - Strengthened cleanup with a baseline max switch time, bounded repair inserts for no-effect tombstones, a final base-table same-SID active-row check before partition drops, and metadata-rich diagnostics when cleanup cannot safely deactivate a snapshot.
  - Added regressions for same-timestamp tie ordering, no-effect tombstone repair, concurrent same-SID publish diagnostics, hidden active rows after an inactive tombstone, and latest-metadata error reporting.

- [x] Startup should not attempt a synchronous ClickHouse tree-summary backfill that can fail under the normal query timeout. It should only schedule/run the provider-owned async tree-summary refresh without `WRSTAT_QUERY_TIMEOUT`, so startup logs show async scheduling/start/completion rather than an expected inline timeout failure.
  - Removed the synchronous startup backfill path: provider startup now captures active mounts, performs only a cheap readiness check, and schedules provider-owned async maintenance when summaries are missing.
  - Added scheduled, started, and completed provider messages so server logs show the background refresh lifecycle.
  - Kept async refresh retry/cancellation behavior and live snapshot readiness updates, and moved lazy reader creation outside the provider mutex to avoid self-deadlock when scheduling refresh work.
  - Added regressions for async-only startup, provider/server messages, lazy-reader deadlock avoidance, and ready-summary fast-path detection.

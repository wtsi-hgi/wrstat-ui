- [x] Now that 0 record one works, but other things fail like the below. Use bugfix skill to fix. It feels like there are no real integration tests for normal real-world usage. Make sure these get added so the basic "watch is run, triggering multiple simultaneous mounts to be summarised, and those actully work" workflow is known to work.
  - Fixed active virtual validation hash mismatches by canonicalizing active-set membership timestamps to the second precision ClickHouse stores and reads back, while preserving full precision for snapshot IDs and completion metadata.
  - Added a real watch-to-concurrent-summarise integration test that captures watch-produced jobs for two mounts, parses their generated commands, runs the summarise stages concurrently against one fresh ClickHouse database, and verifies final output, logs, active mount timestamps, tree readiness, `ListDir`, and `StatPath`.
  - Stabilized semantic spool row comparisons by injecting a DirGUTA reference clock for spool generation instead of weakening the comparison to counts or nonblank hashes.
  - Files: `clickhouse/dguta_writer.go`, `clickhouse/provider.go`, `clickhouse/summarise_spool_loader_test.go`, `clickhouse/tree_summary.go`, `cmd/summarise_spool.go`, `cmd/summarise_spool_test.go`, `cmd/summarise_test.go`, `main_test.go`, `summary/dirguta/dirguta.go`, `summary/dirguta/dirguta_test.go`.
  - Reviewer returned PASS. Focused ClickHouse, cmd, DirGUTA, and watch integration tests passed; `make test`, `make lint`, and `git diff --check` passed.

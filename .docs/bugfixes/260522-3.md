- [x] The committed perf commands can hide the remaining real-world ClickHouse slowness because they time repeated calls against one directory after warmup: they do not separately measure cold/new Disktree folder clicks, repeated `where` command behavior in a fresh process, or the difference between cache-warmed and first-time directory traversal. Add perf coverage that reproduces the observed behavior that cached repeats are fast while new Disktree folders and `./wrstat-ui where` remain slow.
  - Added ClickHouse and Bolt perf operations for first-time Disktree directory walks and fresh-provider `where` timings, plus `--walk-depth` / `--walk-limit` query flags.
  - Added regression coverage that prevents benchmark discovery from warming the timed tree and prevents ancestor-before-descendant timings from masking cold directory costs.
  - Moved `github.com/spf13/pflag` to the direct module requirements so repo-scoped Go test and tidy gates stay clean.
- [ ] ClickHouse-backed existing endpoints are still too slow for first-time Disktree navigation and `wrstat-ui where`: clicking a new Disktree folder remains multi-second/noticeably slow in the web UI, and repeated `./wrstat-ui where` still takes about 30 seconds because process-local tree caching does not help fresh CLI invocations. Make `where` perform much closer to Bolt, no worse than 10% slower, and make new Disktree folder clicks web-responsive, well under a second, using deeper storage/query changes if needed.

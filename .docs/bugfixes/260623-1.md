- [x] I've copied the stats.gz file to `~/output/nfs/20260621-170004_／warehouse／grit_wh01/`. Errors similar to the one I sent are happening for many production mounts. The concept of a "WAIVER" should be removed, since it would always need to be on. Investigate the issue and fix the write amplification or just unconditionally allow it. Use the bugfix skill workflow to make code changes.
  - Investigation: the copied `grit_wh01` sample has 48,180 rows: 984 directory rows and 47,196 file rows, with no duplicate paths, no duplicate directory paths, no synthetic parser rows, and no file hardlinks. Replaying the parser/bucketing/file-type shape predicts 35,502 `wrstat_dir_filter_all` rows and 35,502 `wrstat_child_filter_all` rows over 986 directory facts, i.e. `36.006085x` per table and `72.012170x` combined, matching the production log. No narrow duplicate-write bug was apparent; high amplification is expected for this filter materialisation shape.
  - Fix: removed the full-filter amplification hard-fail and legacy waiver environment handling. High amplification now preserves warning telemetry and staged row-count evidence, then continues publishing.
  - Files: `clickhouse/summarise_spool_loader.go`, `clickhouse/summarise_spool_loader_test.go`, `main_test.go`.
  - Gates: failing-first focused spool-loader test reproduced the old failure; `cleanorder -min-diff clickhouse/summarise_spool_loader.go clickhouse/summarise_spool_loader_test.go main_test.go`; `timeout 300s golangci-lint run --fix`; focused spool-loader test; `timeout 300s make lintnonpm`; `timeout 120s git diff --check`; `timeout 900s make testnonpm` passed.

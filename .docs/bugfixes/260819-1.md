- [x] "Split build from publish" so that the stats.gz processing can be done in parallel, and only the actual clickhouse interaction is limited by a limit group.
  - Required restart edge case (verbatim): "watch being restarted while a publish job is pending: it shouldn't redo the spool-building nor cause an additional publish job to get added."
  - Added resumable build-only and publish-only ClickHouse spool phases; publish fails closed unless a verified durable spool already exists, and only publish performs final timestamping/output promotion.
  - Changed watch scheduling to submit an unrestricted build followed by an essence-dependent publisher carrying the shared ClickHouse limit group, while preserving resource learning, logs, manual guard warnings, and the exported watch API.
  - Added CLI, spool, scheduler, real ClickHouse, and live WR/Bolt restart regressions; the restart case proves one stable pending publisher, no replacement build, unchanged spool artifacts, and successful manifest re-verification.
  - Touched `cmd/summarise.go`, `cmd/summarise_spool.go`, `cmd/summarise_spool_test.go`, `cmd/summarise_test.go`, `cmd/watch.go`, `go.mod`, `main_test.go`, `watch/watch.go`, and `watch/watch_test.go`.
  - `make lint`, `make test`, `git diff --check`, `go mod tidy -diff`, repeated live restart tests, and fresh independent review all passed.

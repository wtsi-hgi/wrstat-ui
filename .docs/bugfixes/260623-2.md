- [x] Fix summarise schema bootstrap lock timeout during ClickHouse close.
  - User report: `summarise` of `/nfs/wrstat/nfs/20260621-170004_／warehouse／pancancerarchive/stats.gz` parsed 20 records, entered `current_phase=clickhouse_close`, then failed after 31s with `clickhouse: timed out waiting for schema bootstrap lock: context deadline exceeded`.
  - Production context: `query_timeout=30s`, `publish=true`, database `prod`, mount `/warehouse/pancancerarchive/`; tiny mounts should not fail just because another process is bootstrapping or repairing the ClickHouse schema.
  - Expected behaviour: waiting for the local schema bootstrap lock must not be bounded by the normal per-query/summarise timeout in a way that causes concurrent production jobs to fail, while still preserving cancellation for genuine shutdowns and avoiding indefinite stuck-lock hangs.
  - Fix: schema bootstrap now waits for the local cross-process lock using a fresh bounded 10-minute bootstrap window, then creates a fresh schema-query context after acquiring the lock instead of inheriting an already-expired per-query context. Explicit `context.Canceled` still interrupts the wait where Go can distinguish it from a deadline expiry.
  - Tests: added regressions for waiting past a caller query deadline, bounded stuck-lock timeout, explicit cancellation, and kept the existing `NewClient` lock wait regression green.
  - Verified: `CGO_ENABLED=1 go test -tags netgo --count 1 ./clickhouse -v -run 'TestEnsureSchemaWithBootstrapLockWaitsPastCallerQueryDeadline|TestSchemaBootstrapLockWaitContext|TestNewClientBootstrapLockWaitDoesNotUseQueryTimeout'`; `CGO_ENABLED=1 go test -tags netgo --count 1 ./clickhouse`; `cleanorder -min-diff clickhouse/schema_bootstrap_lock.go clickhouse/bootstrap_test.go`; `golangci-lint run --fix`.

- [x] `t283_imaging` still fails during DGUTA import after the raw batch cap, now with `block_rows=10000` spending about 10 minutes in `wrstat_dguta_insert` before `connection reset by peer`; make huge DGUTA streams robust by avoiding fragile long-lived flushed DGUTA insert batches while preserving bounded memory and successful row import.
  - Changed full raw DGUTA blocks to `Send()` as discrete prepared batches and then prepare a fresh DGUTA batch, avoiding repeated `Flush()` on one long-lived native insert batch.
  - Added regression coverage for capped DGUTA block sends, final partial batch close sends, and safe terminal error handling if a reprepare fails after a successful send.
  - Proved the fix on a bounded 2M-line local `t283_imaging` import: 18,044,757 `wrstat_dguta` rows imported successfully in 6m29s with max RSS about 933 MiB.

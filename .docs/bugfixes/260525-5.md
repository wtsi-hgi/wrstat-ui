- [x] ClickHouse schema bootstrap fails on production with `clickhouse: failed to execute schema DDL: code: 1, message: System call renameat2() is not supported`; the active-view migration must not use DDL that requires unsupported atomic rename syscalls on the production filesystem.
  - Replaced `CREATE OR REPLACE VIEW wrstat_mounts_active` with an ordered drop-then-create migration that avoids ClickHouse's atomic view replacement path.
  - Added a standalone schema step to drop the legacy `wrstat_mounts_active` view after the `active` column migration and before recreating the view with tombstone semantics.
  - Added regression coverage proving schema SQL contains no `CREATE OR REPLACE VIEW`, preserves the required DDL ordering, and migrates a legacy active view while keeping inactive/latest rows hidden and later active rows restored.

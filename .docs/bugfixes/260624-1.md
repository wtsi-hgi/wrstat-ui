- [x] It's rarer, but it still fails. stats.gz in `~/output/nfs/20260623-170006_／nfs／humgen01`.
  `t=2026-06-24T10:14:39+0100 lvl=info msg="summarise start pid=3561624 input=\"/nfs/wrstat/nfs/20260623-170006_／nfs／humgen01/stats.gz\" output=\"/nfs/wrstat/clickhouse/watch_sb10/db/.20260623-170006_／nfs／humgen01\" current_phase=preflight mount_path=\"/nfs/humgen01/\" mountpoints=\"/nfs/wrstat/mounts\" snapshot_id=b511db6c-9993-51e6-99e8-7aa2a8a7c48e updated_at=\"2026-06-24T04:57:21.472817Z\" clickhouse_database=\"prod\" clickhouse_dsn=\"clickhouse://wrstat:xxxxx@farm22-wrstat01:19001/default?compress=lz4&database=prod&dial_timeout=1s&secure=true&skip_verify=true\" query_timeout=30s signals=\"SIGTERM,SIGINT,SIGQUIT\" note=\"SIGKILL cannot be caught; use the last progress/RSS breadcrumb before an LSF kill\""`
  `t=2026-06-24T10:17:18+0100 lvl=info msg="summarise signal pid=3561624 input=\"/nfs/wrstat/nfs/20260623-170006_／nfs／humgen01/stats.gz\" output=\"/nfs/wrstat/clickhouse/watch_sb10/db/.20260623-170006_／nfs／humgen01\" records=0 elapsed=2m39s current_phase=parse heap_alloc_mb=8106 heap_sys_mb=8299 rss_mb=8540 goroutines=5 gc_count=302 recent_import_phases=\"\" total_import_phases=\"\" mount_path=\"/nfs/humgen01/\" mountpoints=\"/nfs/wrstat/mounts\" snapshot_id=b511db6c-9993-51e6-99e8-7aa2a8a7c48e updated_at=\"2026-06-24T04:57:21.472817Z\" clickhouse_database=\"prod\" clickhouse_dsn=\"clickhouse://wrstat:xxxxx@farm22-wrstat01:19001/default?compress=lz4&database=prod&dial_timeout=1s&secure=true&skip_verify=true\" query_timeout=30s signal=SIGINT"`
  - Regression context: commit `24192d8` made the failure rarer by routing large spools to dirbuild, streaming dirbuild rollups, preserving full-filter counts for child-before-parent emission, and reporting size-based dirbuild routing as `large_unprobed`. This exact `humgen01` file is still killed in `current_phase=parse` around 8.5 GiB RSS with `records=0`, so the next fix must identify the remaining retained parse structure on this large dirbuild path and either bound it or provide evidence that this mount class requires a higher reservation.
  - Fixed the remaining retained structure in `summary/dirguta`: `GUTAStore` no longer stores a full `SummaryWithTimes` in per-directory maps for every `(gid, uid, filetype, age)` key, and instead keeps compact tuple summaries keyed by `(gid, uid, filetype)` plus an age bitmask, materializing age-specific rows only for output/fallback paths.
  - Updated `summary/dirbuild` to use the `GUTAStore` accessors rather than reaching into the old map representation, preserving dirbuild rollup semantics while avoiding the age-bucket map fanout.
  - Added a behavioural dirbuild memory regression for large aged fanout and preserved the prior large-spool, non-contiguous, full-filter child count, and `large_unprobed` behaviours from `24192d8`.
  - Exact-file evidence after the fix: the local `humgen01` file routes as `large_unprobed`; shape scan found 13,863,622 rows, 2,703,942 directories, and 11,159,680 files. A full local spool probe completed in 11m16s with 13,863,622 records, 2,703,944 directories, 77,913,203 full-filter rows, 1,175,082,989 spool bytes, about 4,087 MiB peak heap-in-use growth during `rollup_emit`, and 1,368 MiB final RSS.
  - Implementor and reviewer agents ran focused `summary/dirguta`, `summary/dirbuild`, and `cmd` tests, `golangci-lint`, `git diff --check`, `make lintnonpm`, and `make testnonpm`.

- [x] `wrstat-ui watch` retries can get stuck after `summarise` has already published the ClickHouse snapshot: the retry sees `clickhouse: refusing to rewrite active snapshot` for the same `(mount_path, stats.gz mtime)` and fails forever, and the retry may truncate staging output files before detecting the active snapshot.
  - Added a ClickHouse active-snapshot preflight before `summarise` opens or truncates local output files.
  - Added a watch-only opt-in flag so retries of the exact same active snapshot return success and can finish `touch && mv`, while manual summarise still refuses rewrites.
  - Added behavioural regressions for retry success without output truncation, conservative manual refusal, and watch command generation.

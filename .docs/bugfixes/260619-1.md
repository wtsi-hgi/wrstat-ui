- [x] Another summarise failure to fix the same way you fixed scratch124. The attached production log shows `/nfs/wrstat/lustre/20260617-200026_／lustre／scratch122/stats.gz` for mount `/lustre/scratch122/` parsing to 14,000,000 records, then failing in `current_phase=parse_failed` with `err="non-contiguous directory input"`. The matching local file has been replaced at `/home/ubuntu/output/lustre/20260517-200015_／lustre／scratch122/stats.gz`.
  - Root cause: the retry sorter's component key treated a same-name file path such as `/mnt/test/clash` and directory path such as `/mnt/test/clash/` as the same terminal key, allowing stale/inconsistent snapshot rows to place the file between a directory and its descendants and trigger directory re-entry.
  - Fix: changed the retry sorter to encode path components with explicit file/directory terminal markers while preserving component-wise subtree ordering, synthetic parent rows, duplicate directory-boundary handling, Unicode escape behavior, and schema repair.
  - Regression coverage: added sorter and summarise/spool tests for same-name file and directory entries; the reviewer independently proved the old key fails in a safe temporary copy and the fixed real spool path passes.
  - Gates: focused scratch/unicode/sorter/spool tests, ClickHouse package tests, `make lintnonpm`, `git diff --check`, `make testnonpm`, and `make buildnonpm` all passed.

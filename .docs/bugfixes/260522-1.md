- [x] Investigate what is so problematic with the t283_imaging stats.gz file compared to the lustre stats.gz files and implement a fix. Prove the issue is solved with before and after runs using a sufficiently large subset of the file, or worst case a full run on the entire file, capturing memory and time usage.
  - Found that the sampled t283_imaging data is directory-heavy rather than file-hardlink-heavy: the first 5,000,000 rows contain 2,775,096 directories and no non-directory `nlink > 1` rows, while scratch127 has 329,310 directories and 4 non-directory `nlink > 1` rows in the same row count.
  - Updated DirGUTA child summary merging so transient child `SummaryWithTimes` values are recycled when they are merged into an existing parent key, while summaries moved into the parent remain owned by the parent.
  - Preserved the scratch127 behavior that directory `nlink > 1` rows are not treated as hardlinks.
  - Added a directory-heavy DirGUTA allocation regression and benchmark fixture.
  - Proved the fix on a generated 100,001-row t283-shaped `stats.gz`: ClickHouse import max RSS improved from 309,120 KB to 286,756 KB, and wall time improved from 4.50s to 4.44s. The focused benchmark improved from 440,071,664 B/op and 3,200,218 allocs/op to 281,693,248 B/op and 2,300,377 allocs/op.

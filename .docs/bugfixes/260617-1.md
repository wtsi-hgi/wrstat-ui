- [x] Following the implementation of the overhaul spec, all summarise jobs fail with `non-contiguous directory input` during parse, before any ClickHouse import phase starts. Production example: `/nfs/wrstat/lustre/20260616-200025_／lustre／scratch120/stats.gz` for mount `/lustre/scratch120/` fails after `summarise start` with `current_phase=parse_failed`, `records=0`, and `err="non-contiguous directory input"`.
  - Root cause: ClickHouse `dir_id` / `subtree_end` assignment correctly rejects a directory boundary after it has been closed, but the spool parse path assumed the raw `stats.gz` stream was already DFS/subtree-contiguous. Production stats can revisit a subtree later, so the parser can synthesize a second boundary for the same directory before ClickHouse loading starts.
  - Fix: `buildSummariseSpool` now discards the failed partial spool and retries only the spool-build phase through an output-local external sort of raw stats rows keyed by unquoted path. The sorted retry restores contiguous directory subtrees before the existing summariser/allocator runs, so interval correctness is preserved instead of weakening `ErrNonContiguousInput`. Completed-spool retry/publish behavior is unchanged.
  - Tests: added a command/spool regression with raw stats ordered `/a/`, `/b/`, then `/a/deep/`, proving the final spool has valid catalog intervals and file rows reference the correct directory IDs; added sorter tests for path-key ordering, chunk merge ordering, and equal-key stability.
  - Gates: `cleanorder -min-diff cmd/summarise_spool.go cmd/summarise_stats_sort.go cmd/summarise_spool_test.go cmd/summarise_stats_sort_test.go`; `golangci-lint run --fix`; `CGO_ENABLED=1 go test -tags netgo --count 1 ./summary ./summary/dirguta ./cmd ./clickhouse ./internal/chperf -v -run 'TestDirIDAllocator|TestDirGUTAIDAssignment|TestSummariseStatsSort|TestSummariseClickHouseSpoolRows|TestSummariseClickHouseSpoolRetry|TestClickHouseSummariseSpoolLoader|TestImportTableStatsQuery|TestImportPathTextByteMetrics|TestImportReportEnrichment|TestImportProductionReportEvidence|TestImportJ3AggregateEvidence|TestImportReportOperations|TestImportGuardrailOperations'`; `git diff --check`; `make lintnonpm`; `make testnonpm` all passed.

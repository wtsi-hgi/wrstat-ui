- [x] ClickHouse-backed DiskTree first-level expansion does avoidable per-child round trips: on `/lustre/scratch120/` in the 1,000,000-line-per-mount Lustre subset, the DiskTree-shaped `DirInfo` plus `DirHasChildren` path is about 59 ms while the master/Bolt build is about 1.35 ms on the same imported subset, making existing tree navigation slower.
  - Added optional batched tree storage interfaces for child summaries and child-existence checks, with `db.Tree` falling back to the existing per-directory behavior for stores that do not implement them.
  - Implemented ClickHouse batch summary and `has_children` queries over active mount scopes, and changed DiskTree conversion to request all child `has_children` values together.
  - Added regression tests for `db.Tree` batch summary use, DiskTree `has_children` batching, and ClickHouse batched first-level expansion behavior.
- [x] ClickHouse-backed `db.Tree.Where` on `/lustre/scratch120/` in the 1,000,000-line-per-mount Lustre subset takes about 7.9 s with `splits=2`, while the master/Bolt build takes about 22 ms on the same imported subset; the generic recursive tree path does too many small ClickHouse queries for the existing `where` endpoint.
  - Added an optional `db.Tree.Where` storage fast path so backends can preserve existing recursive semantics without per-directory round trips.
  - Implemented ClickHouse subtree `Where` summaries with grouped SQL over active snapshot rows, then reused in-memory collapse and split recursion to preserve output semantics.
  - Added regressions proving the ClickHouse path avoids recursive query fanout, preserves filtering and deepest-single-child collapse, and still handles ancestor directories spanning pinned active mounts.
- [x] The ClickHouse `Where` fast path still scans and transfers the full filtered subtree before applying `splits`: after the previous fix `/lustre/scratch120/` still takes about 1.8 s with `splits=2` because it reads about 108k directory summaries, while the result only needs the traversed collapse/split frontier.
  - Replaced the ordinary single-mount ClickHouse `Where` full-subtree scan with a cached breadth traversal that batches current-frontier `DirInfo` and child lookups, collapses deepest single-child paths, and expands only the requested split frontier.
  - Kept the existing subtree implementation as the ancestor-scope fallback so pinned active mount ancestor behavior remains unchanged.
  - Added a ClickHouse regression that compares the fast path with the generic `db.Tree` algorithm on a wide/deep tree, asserts no full-subtree summary query is issued, and checks bounded query count.

- [x] `make` results in lots of npm warnings. Fix these. Rather than going one by one, if there's a simple "upgrade all" method try that. Ensure tests don't break as a result. It is prefereable to update to the latest version of a adependency even if it means you have to update some calling code because of a breaking API change. Likewise, do the same for go dependencies, though there may be constraints not worth trying to work around given the commented section at the end of go.mod.
  - Used `npm-check-updates --target latest --upgrade`, then replaced the latest-but-still-deprecated `react-scripts` stack with Vite/Vitest so `npm ci` no longer emits the old CRA deprecation warnings.
  - Updated frontend runtime and tooling dependencies to their latest available versions, regenerated `npm-shrinkwrap.json`, and moved the Vite entry point to `server/static/wrstat/index.html` while preserving the `build` output directory used by Go embedding.
  - Ran `go get -u -t ./...` and kept the existing replacement constraints; all direct Go dependencies are current except `github.com/olekukonko/tablewriter`, which remains pinned to v0.0.5 because the latest available `github.com/VertebrateResequencing/wr` still uses the v0 tablewriter API.
  - Adjusted tests for the newer ClickHouse `Rows.HasData` interface and GoConvey nil/pointer identity behaviour.
  - Verified `make`, `make test`, `go test -tags netgo --count 1 ./...`, `npm run build:prod`, and `npm test`.

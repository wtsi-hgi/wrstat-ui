- [x] Production `./wrstat-ui where --dir /` and default `./wrstat-ui where` now return `bad query; check dir, group, user and type`; `/lustre` works, `/nfs` does not.
  - Added a ClickHouse regression with 100 active `/nfs/projectNNN/` mount roots plus a `/lustre/...` mount, where `/` and `/nfs` are virtual ancestors rather than active mount roots.
  - Fixed `Tree.Where` virtual ancestor traversal so configured-but-inactive parents such as `/nfs` synthesize summaries and traversal children from active mount-root rows instead of falling through to `ErrDirNotFound`.
  - Preserved direct `Children()` behavior for inactive configured parent mounts while keeping active-mount fast paths and existing filter semantics intact.
- [ ] Disktree clicks are generally fast after the recent changes, but a click under `/nfs` can stall and never return when production has around 100 `/nfs` mounts.

- [x] Concurrent ClickHouse jobs can fail with `Unknown table expression identifier 'wrstat_mounts_active'` because schema bootstrap drops and recreates the legacy active view while other processes are reading it; the tombstone-aware active mount migration must not make `wrstat_mounts_active` temporarily disappear.
  - Removed the schema step that dropped the legacy `wrstat_mounts_active` view, eliminating the window where concurrent readers could see the view disappear.
  - Added a new internal tombstone-aware `wrstat_mounts_active_v2` view created with `CREATE VIEW IF NOT EXISTS`, leaving any existing legacy view untouched for compatibility.
  - Updated production ClickHouse queries to read active mounts through `wrstat_mounts_active_v2` while preserving legacy-view references only in schema/tests.
  - Added regression coverage for legacy DB bootstrap, v2 tombstone semantics, and a production-source guard preventing new Go code from querying the legacy view.

- [x] In production all 187 mounts summarised quickly and without error, except for 1. The largest mounts that worked are in ~/output/lustre, including scratch127 which only took ~1h30m. The failing t283_imaging mount is in ~/output/nfs. Use the bugfix skill workflow to fix this, but learn from past mistakes and do not do a narrow fix. Consider all possible timeout issues and prevent them all. Consider what the real underlying cause of why it hitting the timeout in the first place and fix that. You must not regress summarise or query speed or significantly worsen summarise memory usage, so take current and after clickhouse-perf measurements to proove this.
  - Root cause: basedirs `Reset()` eagerly prepared all four native insert batches. On t283-style imports those low-volume batches could remain open across history/finalise side queries and only send at `Close()`, exposing close-time basedirs flush to stale prepared-batch connection/context state and the normal 30s query timeout failure mode.
  - Files: `clickhouse/basedirs_store.go`, `clickhouse/basedirs_store_test.go`.
  - Approach: made basedirs insert batches lazy via the shared import block writer, propagated write errors across basedirs batch slots, avoided preparing empty batches, flushed pending batches before history side queries, and changed finalise to read all quota-history dates before opening age-all usage batches.
  - Coverage: added regressions proving pending basedirs usage flushes before history side queries and finalise reads multi-GID histories before opening age-all usage batches, preventing late close sends after side queries.
  - Perf proof: `.tmp/agent/260603-1-baseline/` and `.tmp/agent/260603-1-after/` used 200k-record local prefixes from full t283 and scratch127 inputs; t283 import improved from 6.54s / 532316 KB RSS to 6.40s / 531932 KB RSS, scratch127 import changed from 1.47s / 243200 KB RSS to 1.58s / 224484 KB RSS, row counts were unchanged, and query timings were broadly stable.
  - Gates: `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./clickhouse ./cmd ./internal/chperf`; `timeout 600s make testnonpm`; `timeout 300s make lintnonpm`; `timeout 120s go mod tidy -diff`; `timeout 120s git diff --check`.

- [x] Some ClickHouse summarise jobs fail after multiple retry attempts with `clickhouse: failed to delete active snapshot mount row: context deadline exceeded`; this may not be the first error, but cleanup of an abandoned active deterministic snapshot should not fail under the normal query timeout.
  - Changed active snapshot cleanup so the common case with an older snapshot rolls `wrstat_mounts_active` back by inserting a newer mount metadata row for the previous snapshot instead of waiting on a synchronous `ALTER TABLE ... DELETE` mutation.
  - Kept the active deterministic snapshot guard and added an explicit post-cleanup check that the failed snapshot is no longer active before failed snapshot partitions are dropped.
  - Moved unavoidable active-row DELETE and partition cleanup work onto a cleanup-specific 30-minute context rather than the normal query timeout.
  - Added regression tests proving rollback avoids the DELETE mutation when an older snapshot exists and unavoidable deletes do not use the normal query timeout.

- [x] Treat post-spool publish as a durable phase machine, with explicit phase markers and retry resume points. Active virtual refresh and all existing publish/finalise work must still run; do not skip anything.
  - Files: `clickhouse/summarise_spool_publish_state.go`, `clickhouse/summarise_spool_loader.go`, `cmd/summarise.go`, `cmd/summarise_spool.go`, `cmd/summarise_spool_test.go`.
  - Added durable `post_spool_publish_state.json` phase markers keyed to the exact spool manifest, mount path, snapshot ID, and update time.
  - Made retries resume completed post-spool phases without rereading `stats.gz`, while still requiring active virtual readiness, mount switch, old snapshot cleanup, old active virtual cleanup, tree summary refresh attempt, active-prefix refresh attempt, table evidence/reporting, and completion marker creation before publish is complete.
  - Stored a switch plan before switching so retries after a post-switch/pre-marker crash can still clean the prior snapshot and active-virtual set safely; preflight now resumes that state instead of cleaning the already-active snapshot.
  - Recomputed active virtual readiness and switch plans when another mount changed the active set before this mount switched, preventing stale active-virtual rows from being trusted.
  - Reviewer returned PASS after two correction cycles. Focused ClickHouse/cmd regressions, `make test`, `make lint`, and `git diff --check` passed.
- [ ] Add fault-injection tests for timeout/failure at every post-spool publish phase, including "fail twice, then retry succeeds without reparsing stats.gz".
- [ ] After the durable publish/retry fixes are committed, investigate why the ClickHouse publish operations are timing out and make the timing-out operations smaller or faster without sacrificing regression tests, behavioural tests, coverage, or required publish/finalise work.

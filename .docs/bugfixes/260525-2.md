- [x] Direct ClickHouse projection writes during summarise now accumulate `wrstat_dir_summary` / `wrstat_dir_dguta_vector` state for the whole mount in Go memory until `Close()`, causing large LSF memory increases: on a 1M-line `/lustre/scratch120` subset max RSS rose from about 0.55 GiB before the direct-projection change to about 6.1 GiB after it. Keep the fresh-import-only direct projection design, but stream/flush projection rows during import so summarise memory is close to the pre-change level and does not require 32-64GB reservations.
  - Reworked the ClickHouse DGUTA writer so maintained mount-dir summary/vector projection batches are prepared with the import batches, appended per `RecordDGUTA`, flushed by batch size during `Add`, and aborted/cleaned up with the rest of the new snapshot on failure.
  - Kept `wrstat_dir_summary_sets` as the close-time readiness marker, so a snapshot only advertises maintained projection readiness after all streamed projection batches have been sent successfully.
  - Added regression coverage proving projection batches stream before `Close` while the summary-set marker is deferred, and kept semantic coverage for all/default summaries, file-only summaries, child counts, arbitrary filters, and the no-ClickHouse-rebuild rule.
  - Verified on the 1M-line `/lustre/scratch120` subset that max RSS dropped from the previous direct-projection baseline of about 6.1 GiB to 1,708,216 KB (about 1.63 GiB), with the import completing in 35.15s.

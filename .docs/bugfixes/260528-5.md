- [x] Long-running ClickHouse summarise imports can still fail after many successful bounded sends with `clickhouse: failed to send dguta batch: context deadline exceeded`, as in production `scratch127` after 130,000,000 records; take a holistic view of import batch send/prepare/connection timeout semantics so DGUTA, children, projections, files, and basedirs cannot fail late because a send path still uses the normal query timeout or an unrecoverable long-lived batch/connection state, while preserving bounded memory, snapshot safety, and previous cleanup fixes.
  - Files: `clickhouse/client.go`, `clickhouse/dguta_writer.go`, `clickhouse/mount_dir_projection_writer.go`, `clickhouse/file_ingest_operation.go`, `clickhouse/basedirs_store.go`, `clickhouse/import_context_test.go`, `clickhouse/client_options_test.go`.
  - Approach: audited clickhouse-go v2.46.0 batch semantics, removed `WithReleaseConnection()` from shared import batches, detached import prepare/send from normal query deadlines, and normalized only import writer connections with safe dial/read/lifetime/open/idle floors.
  - Capacity note: the import pool floor is five connections, covering the four long-lived basedirs prepared batches plus one control/query connection for history and finalise side queries.
  - Coverage: DGUTA, children, dir summary/vector projection, dir summary set, files, basedirs, reprepare helpers, basedirs low-pool history/finalise side queries, and final close sends now share the import batch helper and import-safe connection options.
  - Proof: `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./clickhouse ./cmd ./internal/chperf`; `timeout 600s make testnonpm`; `timeout 300s make lintnonpm`; `timeout 120s go mod tidy -diff`; `timeout 120s git diff --check`.

- [x] Summarise jobs now fail with ClickHouse close error ``code: 497, message: wrstat: Not enough privileges. To execute this query, it's necessary to have the grant SELECT(`table`, rows, data_compressed_bytes, data_uncompressed_bytes, database, active) ON system.parts``.
  - Fixed as a bug, not a new required production ClickHouse grant: normal summarise spool-load reporting now treats missing `system.parts` privileges as unavailable optional table-size evidence instead of failing close.
  - Files: `clickhouse/summarise_spool_loader.go`, `clickhouse/summarise_spool_loader_test.go`.
  - Added regressions proving ClickHouse code 497 from the `system.parts` table-stats query preserves load/count/publish evidence and succeeds, while non-497 table-stats errors still fail.
  - Reviewer returned PASS. Focused spool-loader, final-gate, `cmd`, `internal/chperf`, lint, tidy, and diff-check gates passed; full `./clickhouse` and `make testnonpm` gates timed out in unrelated long-running ClickHouse tests while still progressing.

- [x] Another summarise failure: `clickhouse: failed to send dir dguta vector batch: code: 209, message: Timeout exceeded while receiving data from client. Waited for 310 seconds, timeout is 300 seconds.`
  - Added regression coverage for lazy DGUTA, children, dir summary, and dir dguta vector batch preparation; immediate post-send reprepare is no longer expected.
  - Added fake-clock regression coverage proving slow partial raw, children, projection, and standalone projection helper batches are sent before the ClickHouse receive-timeout window.
  - Implemented just-in-time import batch preparation and age-guarded sends so long-running summarise imports do not hold idle prepared INSERTs open.
  - Gates:
    - `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./clickhouse -run 'TestClickHouseImportBatchContexts|TestDGUTAWriter|TestMountDirProjection|Test.*Batch|Test.*Receive|Test.*Import'`
    - `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./clickhouse ./cmd ./internal/chperf`
    - `timeout 600s make testnonpm`
    - `timeout 300s make lintnonpm`
    - `timeout 120s go mod tidy -diff`
    - `timeout 120s git diff --check`

- [x] Give the ClickHouse recovery flag a better shorter name.
  - Renamed the public summarise retry-recovery flag to `--clickhouse-recover`.
  - Updated watch-generated summarise commands and tests to use the new flag.
  - Removed the temporary alias because it was never deployed.
- [x] A long-running `wrstat-ui summarise` job can fail after about an hour with `clickhouse: failed to send files batch: dial tcp: lookup farm22-wrstat01: i/o timeout`, as seen for `/nfs/wrstat/nfs/20260514-170002_／nfs／t283_imaging/stats.gz` on attempt 5 after 1h 38s; investigate and fix so retries/ingest handle this edge case robustly.
  - File ingest now prepares ClickHouse file batches only immediately before sending buffered rows, so a replacement batch is not held idle across long summarise work.
  - A failed/ambiguous files batch `Send` is remembered so `Close` does not retry those rows and risk duplicate inserts.
  - Added regression coverage for lazy batch preparation across flush boundaries, no close-time retry after an ambiguous send error, and active snapshot rechecks after an initial batch prepare failure.

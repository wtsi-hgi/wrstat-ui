- [x] Now it failed pretty quickly like this: `error parsing quotas file: strconv.ParseUint: parsing "-1": invalid syntax`. The problematic quotas file is in `~/output/lustre/quotas-1`; production generates quotas daily from latest info and can have strange info sometimes, so summarise must cope appropriately.
  - Treated generated numeric negative gid rows as sentinel/unknown-group quota rows: they are skipped instead of aborting preflight or wrapping into a `uint32` gid.
  - Normalised numeric negative quota size/inode fields to zero for otherwise valid rows while preserving errors for malformed rows and non-numeric quota fields.
  - Added regression coverage for `-1` gid rows, no wrapped `math.MaxUint32` storage, and numeric negative quota fields.
  - Verified `/home/ubuntu/output/lustre/quotas-1` parses successfully through `basedirs.ParseQuotas`.
  - Gates:
    - `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./basedirs -run 'TestQuotas|TestOwners'`
    - `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./internal/summariseutil ./cmd ./clickhouse ./internal/chperf`
    - `timeout 600s make testnonpm`
    - `timeout 300s make lintnonpm`
    - `timeout 120s go mod tidy -diff`
    - `timeout 120s git diff --check`

- [x] `~/output/nfs/20260517-170004_／nfs／t283_imaging` previously summarised in production, taking over 40GB and more than 12 hours, but after the fresh-ClickHouse/direct-projection changes now exits non-zero with no stdout/stderr even with a 64GB reservation and one-day runtime. Investigate the silent failure mode, identify whether it is application error, ClickHouse failure, LSF kill, memory growth, timeout, corrupt input, or missing diagnostics, then fix the underlying issue and/or the missing diagnostics so this mount can be summarised reliably again.
  - Reproduced the t283-specific pressure on bounded prefixes only: 1M rows expanded to 4,516,772 DGUTA rows and 2,946,425 dir-summary rows; 2M rows expanded to 18,044,757 DGUTA rows and 11,768,795 dir-summary rows, with no corrupt-input or immediate ClickHouse error.
  - Found that direct projection batches only flushed after a whole `RecordDGUTA`, so a directory-heavy record could overshoot the configured batch size and the writer repeatedly finalized/reprepared insert batches instead of using ClickHouse native `Flush()`.
  - Changed DGUTA, children, dir-summary, and dir-vector inserts to flush full native batches in place while appending records, preserving close-time finalisation and snapshot cleanup semantics.
  - Added a regression proving large records cannot create oversized DGUTA/children/dir-summary batches, and added summarise progress diagnostics every 1M parsed rows with elapsed time and Go heap figures so future LSF/process deaths leave a useful stderr trail.
  - Proved the fix on the 2M-row t283 prefix: wall time improved from 3m35s to 2m23s with identical row counts; projection phase improved from 131.99s to 90.30s. A full 5.7GB compressed import was not run because the bounded prefixes reproduced the affected projection path and showed bounded memory without needing the whole file.

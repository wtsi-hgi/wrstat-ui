- [x] A summarise close can switch the active snapshot, spend the full query timeout in best-effort `wrstat_tree_summary_refresh`, then fail mandatory old snapshot cleanup with `clickhouse: failed to drop partition: context deadline exceeded`; make best-effort tree-summary refresh unable to consume the close context needed for required cleanup, so first attempts do not fail and retries are not needed.
  - Reordered snapshot close so old snapshot partitions are dropped immediately after the active mount switch, before best-effort tree-summary refresh can consume the close context.
  - Added a regression test with a previous active snapshot and a tree-summary refresh that exhausts the context; the previous snapshot drop now completes first and the close succeeds.
  - Preserved existing behaviour where tree-summary refresh timeout is ignored as best-effort, while mandatory cleanup errors are still returned.

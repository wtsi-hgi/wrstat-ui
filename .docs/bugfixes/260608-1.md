- [x] After implementing this spec, summarise broke for many small mounts (at least when lots are running in parallel). Example output shows ClickHouse close failing with `clickhouse: failed to delete retry basedirs history rows: context deadline exceeded` after parsing completes.
  - Files: `clickhouse/summarise_spool_loader.go`, `clickhouse/summarise_spool_loader_test.go`.
  - Changed retry basedirs-history cleanup during spool publish to use the existing long cleanup timeout instead of the normal query timeout, preserving exact-key idempotent deletion and real error propagation.
  - Capped schema2 spool replay batches for `wrstat_dir_facts` and `wrstat_children` to the same limits used by direct import, avoiding oversized replay batches during many small parallel retry loads.
  - Added regression coverage for cleanup timeout selection, cleanup error propagation, and schema2 replay batch caps; reviewer returned PASS. `make lintnonpm` passed, while `make testnonpm` was blocked only by an existing unrelated `TestTreeRESTVirtualAncestorRoutesC3` server failure.

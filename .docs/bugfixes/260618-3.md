- [x] A summarise job for `/nfs/wrstat/lustre/20260617-200026_／lustre／scratch124/stats.gz` on mount `/lustre/scratch124/` still fails with `non-contiguous directory input` after the unordered-stats retry work. The actual `stats.gz` is now available locally at `/home/ubuntu/output/lustre/20260517-200015_／lustre／scratch124/stats.gz`; a minimal reproduction is the 85-line window starting at raw line 11,345,629 (`WRSTAT_PROBE_START=11345628`, `WRSTAT_PROBE_LIMIT=85`, with synthetic ancestors), where the 85th raw row is `chr1\u00a0/` and the sorted retry re-enters `chr1`/`chr1<NUL>` because stats path unquoting decodes hex escapes incorrectly.
  - Root cause: the custom `stats.unquote` hex decoder treated `A-F` and `a-f` as values `0-5` instead of `10-15`. A literal `\u00a0` path therefore decoded to U+0000, which collided with the sorted-retry component key separator and let the retry stream close `chr1/`, visit `chr1<NUL>/`, then synthesize/re-enter `chr1/`.
  - Fix: hex letters in `stats.unquote` now add the hexadecimal letter offset, preserving `\u00a0` as UTF-8 NBSP. The existing sorted retry behaviour for duplicate boundaries, synthesized parents, component preorder, and legacy schema repair is unchanged.
  - Regression coverage: added `TestUnquote` coverage for `\u00a0`; added `TestSummariseStatsSort` coverage proving escaped unicode siblings sort after the plain-prefix subtree; added `TestSummariseClickHouseSpoolRows` coverage proving the sorted retry spools such rows through the real parser/summariser/dir-id allocator without a NUL catalog path or subtree re-entry.
  - Gates: failing first run `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./stats ./cmd -run 'Test.*Unquote|TestSummariseStatsSort|TestSummariseClickHouseSpoolRows' -v` failed with `TestUnquote` decoding `\u00a0` to byte `0`, `TestSummariseStatsSort` placing `chr1\u00a0/` before `chr1/file.dat`, and `TestSummariseClickHouseSpoolRows` returning `non-contiguous directory input`; `cleanorder -min-diff stats/stats.go stats/stats_test.go cmd/summarise_stats_sort_test.go cmd/summarise_spool_test.go` passed; `timeout 300s golangci-lint run --fix` passed with `0 issues`; `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./stats ./cmd -run 'Test.*Unquote|TestSummariseStatsSort|TestSummariseClickHouseSpoolRows' -v` passed; `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./clickhouse ./cmd ./internal/chperf -v` passed; `timeout 300s make lintnonpm` passed with `0 issues`; `timeout 120s git diff --check` passed; first `timeout 900s make testnonpm` run failed in `clickhouse` because the embedded ClickHouse test server hit `Address already in use` on an ephemeral localhost port, and the immediate rerun of the same command passed.

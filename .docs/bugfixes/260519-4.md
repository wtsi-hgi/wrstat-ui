- [x] `--clickhouse-recover` can treat any matching active ClickHouse snapshot as retry success even when the previous `summarise` attempt did not complete the full stats.gz import, allowing a failed or partial staging directory to be moved into the completed output path.
  - Added a hidden summarise completion marker containing the mount path and stats mtime.
  - Changed retry success so an already-active ClickHouse snapshot is accepted only when the marker matches; otherwise it still refuses before opening/truncating outputs.
  - Added regressions for absent marker refusal, matching marker recovery, manual refusal, successful marker creation, and failed-close marker suppression.

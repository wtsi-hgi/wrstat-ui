- [x] `where` remains too slow on ClickHouse: `time ./wrstat-ui where` still takes around 25s cold and around 5s cached, while Bolt production is around 5s. Reproduce the gap with committed perf subcommands on the same dataset, then implement query/storage/cache improvements until ClickHouse `where` is close to or better than Bolt performance.
  - Changed ClickHouse `whereTraversal` mount summary loading to use the existing grouped dir-summary path for large batches, falling back to raw DGUTA batches for small batches.
  - Extended grouped dir-summary filtering to support file-type bitmasks, preserving `where`'s default non-directory filter without loading raw GUTA rows.
  - Added regression coverage proving large `Where` child batches use grouped summaries and avoid raw DGUTA batch queries, while small batches keep the raw path.
  - Extended maintained per-mount `wrstat_dir_summary` rows with exact default-filter non-directory aggregate columns and bumped the summary version so existing DBs refill them.
  - Taught maintained summary lookup and `Where` traversal to use those non-directory columns for the default `where` filter while preserving all-types summaries and filtered fallback paths.
  - Added cache-key separation for all-types versus non-directory maintained summaries and behavioural coverage for both modes.

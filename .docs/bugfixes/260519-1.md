- [x] `clickhouse-perf import` defaults to `--batchSize 10000` while production `summarise` and the ClickHouse ingest spec use 100000; on the copied lustre data, 1M lines per mount with 10k batches took ~137s and created ~10x more ClickHouse parts than 100k batches, while 100k finished in ~33s.
  - Updated `cmd/clickhouse_perf.go` so the import CLI default batch size is 100000.
  - Updated `main_test.go` so `clickhouse-perf import --help` must advertise the 100000 default and reject the old 10000 flag line.
- [x] ClickHouse schema bootstrap treats duplicate `version=1` rows in `wrstat_schema_version` as fatal even though the bootstrap lock is local to a node; the first production watch batch against an empty database can race across HPC nodes, insert duplicate version rows, and poison future ClickHouse startup.
  - Updated `clickhouse/schema.go` so duplicate identical `version=1` rows are accepted while mixed or unsupported versions still fail validation.
  - Added a regression test in `clickhouse/bootstrap_test.go` for duplicate existing `version=1` rows, plus guards for mixed and unsupported versions.
  - Verified with `CGO_ENABLED=1 go test -tags netgo --count 1 ./clickhouse -v -run TestEnsureSchemaVersion`, `timeout 120s go test -tags netgo --count 1 ./clickhouse`, and `timeout 120s golangci-lint run --timeout 2m ./clickhouse`.

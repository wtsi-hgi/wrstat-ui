- [x] Use the bugfix skill workflow to fix any failing test or lint issues.
  - Files: `server/db.go`, `server/where.go`, `server/provider_test.go`, `server/server_test.go`, `server/tree_batch_test.go`.
  - Fixed the failing `TestTreeRESTVirtualAncestorRoutesC3` root cause where replacing the server provider with a cold provider using the same active-set ID could reuse stale response-cache JSON and produce zero ClickHouse query deltas.
  - Added a mutex-safe response-cache clear on provider assignment/refresh and a regression proving same-active-set provider replacement returns fresh results.
  - Kept C3.5 comparison metadata and deterministic query-count improvement checks while removing the flaky hard requirement that a tiny 5-sample wall-clock P95 always improves.
  - Reviewer returned PASS. `make lintnonpm`, focused server regressions, `make testnonpm`, and `git diff --check` passed.

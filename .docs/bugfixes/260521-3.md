- [x] The scratch `.tmp/agent/treebench/main.go` harness needed for comparing Bolt and ClickHouse tree endpoint timings is not committed functionality: `clickhouse-perf query` does not time `tree_where`, neither perf command reports the DiskTree-shaped `DirInfo` plus child `has_children` path, and future runs should be possible from the perf subcommands without private helper code.
  - Added `tree_where` timing to `clickhouse-perf query`, with committed `--splits` and `--warmup` controls.
  - Added `tree_disktree_endpoint` timing to ClickHouse and Bolt perf query reports, using `Tree.DirInfo` followed by `Tree.DirsHaveChildren` over all children so `db.Tree` handles batch and fallback behavior.
  - Added regression coverage for ClickHouse operation reporting, warmup behavior, command flags, Bolt operation reporting, DiskTree fallback behavior, and integration report operation lists.
- [x] ClickHouse-backed DiskTree child `has_children` checks still do avoidable ClickHouse work after batching: `DirsHaveChildren` fetches all grandchildren and then fetches summaries for those grandchildren, when it only needs to know whether each child has at least one filter-passing child directory.
  - Replaced the ClickHouse `DirsHaveChildren` active-mount path with a grouped parent-existence query joining `wrstat_children` to `wrstat_dguta`, so it answers child existence without fetching all grandchildren or summarising them individually.
  - Preserved filter behavior for nil filters, nil versus empty GID/UID lists, FT bitmasks, exact Age matching, and `count > 0`, while keeping unresolved/ancestor fallback behavior unchanged.
  - Added regression coverage proving the fast path avoids child summary batch queries and preserves active snapshot scoping, duplicate/unsorted input handling, missing dirs, and filter semantics.
- [x] The grouped ClickHouse `DirsHaveChildren` join introduced for child existence can be slower than the previous batched path when the requested parents have no children, because the query may still pay DGUTA join/planning cost instead of cheaply returning false after the child lookup.
  - Added a cheap scoped `wrstat_children` batch lookup before the DGUTA existence join and return all false immediately when requested parents have no child rows.
  - Kept the filtered existence join for parents with candidate children, preserving the prior no-child-summary-query fast path and filter semantics.
  - Added regression coverage proving no-child parents avoid the DGUTA existence join and still return false for every requested parent.
- [x] The grouped ClickHouse `DirsHaveChildren` join is also slower than the previous batched summary path when there are only a few candidate child directories: on the 1M-line `/lustre/scratch120/` subset the child lookup finds three grandchildren and the existence join takes roughly 340 ms, while fetching summaries for those three child directories is around 10-20 ms.
  - Made `DirsHaveChildren` adaptive after the scoped child lookup: no candidates still short-circuit, small candidate sets use batched `DirInfos`, and larger candidate sets keep the grouped existence join.
  - Added regression coverage proving small fanout uses one summary batch and no existence join, while the large-fanout case still uses the join and avoids child-summary fanout.

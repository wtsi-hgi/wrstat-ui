- [x] Fix summarise parse-phase memory spikes above 8GB.
  - User report: production `summarise` for `/nfs/wrstat/lustre/20260622-200025_／lustre／scratch120/stats.gz` logged `summarise signal` after 1m22s in `current_phase=parse` with `records=0`, `heap_alloc_mb=7557`, `heap_sys_mb=8295`, `rss_mb=8517`, then received `SIGINT`; jobs are being killed for exceeding an 8GB memory limit.
  - Production context: `mount_path="/lustre/scratch120/"`, database `prod`, `query_timeout=30s`, ClickHouse spool output enabled. The process should not require more than the job memory limit before the first useful parse progress breadcrumb.
  - Expected behaviour: ClickHouse summarise parse/build remains memory bounded for huge production stats inputs, including inputs that ultimately need the dirbuild/non-contiguous path, and diagnostics should give useful evidence before a kill where practical.
  - Root cause: ClickHouse spool builds always attempted the memory-heavy contiguous fast parser first and only retried with dirbuild after `summary.ErrNonContiguousInput`, so huge production inputs could exceed the job memory limit before reaching either the fallback or the first 1,000,000-row progress breadcrumb.
  - Fix: ClickHouse spool builds now route stats files of at least 512 MiB directly to the bounded dirbuild path, preserving the existing fast path for small contiguous inputs and the fallback retry for smaller non-contiguous inputs.
  - Regression coverage: added a cmd-level test that fakes a huge `stats.gz` without allocating one and proves the build invokes dirbuild first, opens only for dirbuild's two passes, and records `input_shape=non_contiguous` plus `build_path=dirbuild`.
  - Verification: `CGO_ENABLED=1 go test -tags netgo --count 1 ./cmd -v -run 'TestSummariseClickHouseSpoolRows|TestSummariseClickHouseSpoolB2Retry'`; `CGO_ENABLED=1 go test -tags netgo --count 1 ./summary/dirbuild -v -run 'TestBuildA4BoundedMemoryAndBytesWritten'`; `CGO_ENABLED=1 go test -tags netgo --count 1 ./cmd`; `cleanorder -min-diff cmd/summarise_spool.go cmd/summarise_spool_test.go cmd/summarise_test.go`; `golangci-lint run --fix`.

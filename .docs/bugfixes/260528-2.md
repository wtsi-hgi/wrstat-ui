- [x] Summarise can publish a new ClickHouse snapshot successfully, then fail during `old_snapshot_partition_drop` because previous snapshot partition cleanup uses the normal 30s query timeout and times out after ~28s with `failed to drop partition ... i/o timeout`; use the long cleanup timeout for mandatory old snapshot partition cleanup after a successful switch so published snapshots do not get retried just because cleanup takes longer than a normal query.
  - Changed previous-snapshot partition cleanup after a successful switch to use the existing 30-minute cleanup context instead of the normal query timeout.
  - Added a regression test that rejects normal-query-timeout partition drops and verifies all old snapshot partition drops use the cleanup deadline.
  - Preserved the old-snapshot idempotency check, cleanup-before-tree-refresh ordering, and real error propagation.

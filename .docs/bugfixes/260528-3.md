- [x] Huge ClickHouse summarise imports can still fail when any remaining long-lived native insert batch or normal-timeout partition cleanup path is hit: `scratch122` exposed both `wrstat_children_insert` broken pipes and repeated `partition_drop_reset` / failed-snapshot cleanup timeouts; audit and fix the whole summarise write/cleanup surface so all high-volume insert batches use bounded discrete sends and all partition-drop cleanup/reset paths use cleanup-timeout semantics, preserving final close semantics, idempotency, and real error propagation.
  - Audited DGUTA, children, dir summary, dir DGUTA vector, file ingest, and basedirs write paths; children now use capped discrete `Send()` blocks with fresh batches, matching the already-bounded raw DGUTA, projection, file ingest, and basedirs paths.
  - Centralised partition-drop cleanup-timeout semantics in `dropPartitionIgnoreUnknown`, covering DGUTA reset/cleanup, old snapshot drops, active snapshot cleanup, file ingest reset, basedirs reset/abort cleanup, and projection reset while preserving unknown-partition idempotency and real error propagation.
  - Added regression coverage for children send/reprepare/final partial close behaviour and for normal-query-timeout partition-drop contexts through the shared helper and file-ingest reset callsite.
  - Verified with the full gates and a bounded 20k-line local `scratch122` proof against a throwaway ClickHouse database: files=20000, dguta=19408, children=1687, dir_summary=11821, dir_vector=1688.

- [x] have the --clickhouse-build-only and --clickhouse-publish-only jobs in different requirements groups, and have the build jobs in a nice short,meaningful-yet-unique dep group (what's used for the rep group, appended with "build"?) that the publish job can depend on, rather than using the full command line to define the dependency.
  - Split the existing per-mount requirements group into stable `-build` and `-publish` groups so WR learns the phases' resource needs independently.
  - Gave each build/publish pair one timestamped reporting group containing WR's purpose-built 20-character unique token; the build exposes `<reporting-group>-build` and publish depends only on that dependency group, with no command essence.
  - Added deterministic and live WR/Bolt regressions for same-target/same-time distinct job graphs, pair-specific publisher release, unrelated pending builds, restart behavior, and reporting/resource group shape.
  - Touched `watch/watch.go`, `watch/watch_test.go`, and `main_test.go`; focused/stress tests, `make lint`, `make test`, `git diff --check`, `go mod tidy -diff`, and fresh independent review passed.
- [x] Also, is there a benefit or need for the command lines to have summarise_log calculated during the execution, instead of when the commands are added to wr for simpler, shorter, more readable commands? If not, improve this as another bugfix item.
  - Kept runtime calculation: WR retries and copied/manual runs execute the unchanged stored command, and WR exposes no per-attempt interpolation or environment value, so a submission-time filename would be reused and overwrite or merge attempt diagnostics.
  - The existing behavioral regression executes one stored command through a failed then successful attempt and proves distinct combined stdout/stderr logs, shell-safe paths, and promotion; testing-principles does not justify a source-shape test or cosmetic production change.
  - No source/test files changed; the focused regression, `make lint`, `make test`, `git diff --check`, `go mod tidy -diff`, and fresh independent review passed.
- [ ] Runtime summarise log filenames can collide on shared storage when attempts or phases start in the same second on different hosts with the same shell PID.
  - Evidence: WR retries the unchanged stored command and supplies no per-attempt identifier; the current filename contains only second-resolution UTC time and `$$`, whose PID namespace is host-local.

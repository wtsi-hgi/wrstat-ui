- [x] ClickHouse arbitrary-filter tree traversal remains slower than Bolt because the maintained summaries only cover collapsed all-files and default non-directory projections. Add a Bolt-like maintained per-directory DGUTA-vector projection, remove earlier cautious query/storage paths that become redundant, and keep iterating on the query implementation until default and arbitrary-filter Disktree/`where` timings are no worse than Bolt on the same datasets, with no regression to the currently fast unfiltered paths.
  - Added a maintained `wrstat_dir_dguta_vector` projection with one row per active mount directory, aligned DGUTA arrays, child counts, partition cleanup, schema/bootstrap coverage, and async provider-owned projection refresh so existing databases backfill without normal query-timeout abandonment.
  - Routed arbitrary UID/GID/file-type tree filters through the vector projection for ordinary `DirInfo`/Disktree paths, while preserving the existing maintained summary fast path for broad/default filters.
  - Added a selective mount-root arbitrary-filter `Where` preload path that loads matching summaries in one filtered aggregate and only matching child edges, avoiding cold full-vector traversal.
  - Added `clickhouse-perf`/`bolt-perf` tree filter flags and report inputs for filtered `where`/Disktree proof runs; legacy Bolt interface mode now rejects unsupported tree filter flags.
  - Verified on the same 1.67M-line `/lustre/scratch127/` subset: default ClickHouse `tree_where_fresh_provider` p50 47.969 ms versus Bolt p50 96.700 ms, filtered ClickHouse (`gid=706 uid=13847 ft=32768`) p50 170.383 ms / p95 241.151 ms versus Bolt p50 216.474 ms / p95 230.840 ms, and ClickHouse filtered new-directory Disktree p50 12.129 ms.

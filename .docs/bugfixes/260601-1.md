- [x] Despite a recent attempt at improvement, t283_imaging summarise still doesn't complete within 24hrs. Do a better investigation in to what is going on that it can take that long, when the similar sized scratch127 takes less than 3hrs. Examples of both stat.gz files are in ~/output. Improve the speed dramatically via the bugfix skill workflow. There needs to be an order of magnitude change, and it seems more like there is a significant bug here to fix, or we need a radical rethinking of how this is done.
  - Bounded fixture probes over the first 1,000,000 parsed records found no hardlink repeats/collisions and no path-order backtracking in either dataset. The asymmetry is directory and age-row amplification: t283 emitted 230,308 dirs and 4,516,975 DGUTA rows, while scratch127 emitted 46,921 dirs and 1,061,362 DGUTA rows. The first 100,000 records were even more skewed: t283 emitted 35,004 dirs/701,282 DGUTA rows versus scratch127's 1,143 dirs/10,695 DGUTA rows.
  - The pathological cost was raw and maintained-summary materialisation of every age bucket for every internal mount directory. t283's directory-heavy NFS tree has many directory-only summaries, so the writer was asking ClickHouse to ingest roughly 17-23 age rows per logical directory in both `wrstat_dguta` and `wrstat_dir_summary`.
  - Implemented internal mount-directory age compaction: internal raw DGUTA and maintained dir-summary rows now keep the `DGUTAgeAll` row, while `wrstat_dir_dguta_vector` still receives the full GUTA vector for age/GID/UID/FT-correct filtered queries. Mount root and ancestor rows remain full-age so active tree summaries keep their cross-mount behaviour.
  - Updated query routing so non-`DGUTAgeAll` mount summaries use the maintained vector instead of relying on compacted raw rows, and prevented whole-mount `where` preloading from using compacted raw rows for non-all ages.
  - Routed vector-capable active mount-root child checks through vector-backed summaries so age/GID/UID/FT filters still work after raw age-row compaction.
  - Normalised import mount paths before compaction checks so mount roots and sibling prefix paths are not accidentally compacted.
  - Added regression coverage proving a t283-shaped internal-directory workload collapses raw DGUTA and dir-summary rows from 1,700 to 100 while preserving one vector row per directory, and added query coverage for age-only vector summaries, active mount-root child checks, mount-path boundary safety, and chperf import row-count coverage.
  - Gates:
    - `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./clickhouse -run 'TestDGUTAWriter|TestMountDirProjection|TestClickHouseImportBatchContexts|Test.*Batch|Test.*Projection|Test.*Receive|Test.*Import|Test.*Perf|TestClickHouseDatabaseMountDirSummary|TestClickHouseDatabaseDirsHaveChildren'`
    - `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./internal/chperf -run 'TestImportReportOperations|Test.*Import.*Guardrail|TestBuildOps|TestStartupCacheWarmingAudit|Test.*Query|Test.*File|TestDGUTARowCounting'`
    - `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./internal/summariseutil ./cmd ./clickhouse ./internal/chperf`
    - `timeout 600s make testnonpm`
    - `timeout 300s make lintnonpm`
    - `timeout 120s go mod tidy -diff`
    - `timeout 120s git diff --check`

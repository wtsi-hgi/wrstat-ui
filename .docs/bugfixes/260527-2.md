- [x] Before attempting another memory/performance fix for the large `t283_imaging` ClickHouse summarise failure, improve stderr diagnostics so a future non-zero exit, timeout, SIGTERM, or LSF kill leaves enough context to identify the likely failure point: input/mount/snapshot, parsed-row progress, Go heap and process RSS, current summarise/import phase, recent phase durations, and signal/crash breadcrumbs.
  - Added summarise diagnostics that log safe start context, deterministic snapshot ID, input/output/mount/mountpoints, redacted ClickHouse DSN/database, query timeout, phase state, parse/close/completion/failure breadcrumbs, and explicit SIGKILL caveat text.
  - Extended progress breadcrumbs to include parsed records, elapsed time, Go heap, Linux `/proc/self/statm` RSS when available, goroutines, GC count, current phase, and recent import phase timings.
  - Wired ClickHouse file/DGUTA import phase recorders into summarise diagnostics, added concrete basedirs store phase recording for reset/usage/subdir/history/finalise/flush paths, and added catchable SIGTERM/SIGINT/SIGQUIT breadcrumbs with the last known state.
  - Added behavioural regression tests for safe logging/redaction, progress/RSS/phase output, signal breadcrumbs, and portable RSS parser fallback.

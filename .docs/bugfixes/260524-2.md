- [x] Starting the server schedules and starts the async active mount dir projection refresh, but after several minutes it fails with `failed to refresh active mount dir projections asynchronously: clickhouse: failed to refresh dir summaries: query processing: failed to read packet ... read tcp ... i/o timeout`; the async refresh must be able to run to completion instead of hitting the ClickHouse driver's read deadline.
  - Added a provider-owned maintenance ClickHouse connection for async tree-summary and mount-dir projection refreshes, separate from normal foreground/provider reader connections.
  - Wrapped the maintenance connection so the ClickHouse driver's read-deadline calls cannot impose the default socket read timeout on long maintenance reads, while normal queries keep their configured read-timeout behavior.
  - Added regression coverage proving short foreground `read_timeout` settings remain in effect, provider close shuts down the maintenance connection, and the actual `tryMountDirProjectionRefresh`/`tryTreeSummaryRefresh` paths fail if they accidentally use `p.conn` instead of the maintenance connection.

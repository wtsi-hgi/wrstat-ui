- [x] Use the bugfix skill workflow to implement schema2 properly. The whole point was to improve performance of summarised data, so if the performance improvements aren't accessed when summarising data, only on some artifical test path, this is not a "gap", this is "the change did nothing".
  - Files: `internal/chspool/spool.go`, `cmd/summarise_spool.go`, `cmd/summarise_spool_test.go`, `clickhouse/summarise_spool_loader.go`, `clickhouse/summarise_spool_loader_test.go`, `clickhouse/active_prefix_rollups.go`, `clickhouse/database.go`, and `clickhouse/dguta_writer_test.go`.
  - Added first-class production spool rows, manifests, decode counts, load queries, batch caps, and count verification for `wrstat_dir_filter_ageall` and `wrstat_parent_facts`; bumped the spool schema marker so old v1 spools are rebuilt instead of silently publishing without schema2 tables.
  - Made active-prefix readiness wait for both canonical fact readiness and AgeAll source availability, preventing filtered rollups from publishing a ready marker with empty AgeAll data.
  - Added behavioural regressions proving production spool build/replay populates schema2 derived tables, missing-table manifests are rejected/rebuilt, active-prefix refresh has usable AgeAll data after spool publish, and readiness is withheld when AgeAll rows are absent.
  - Reviewer returned PASS. Gates passed: focused provider test, targeted spool/active-prefix regression set, `go test ./internal/chspool ./cmd ./clickhouse ./internal/chperf`, `make testnonpm`, `make lintnonpm`, `go mod tidy -diff`, and `git diff --check`.

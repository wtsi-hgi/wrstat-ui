- [x] Still a problem: production `summarise` for `/nfs/wrstat/nfs/20260621-170004_／warehouse／cellgeni/stats.gz` starts at `2026-06-23T16:11:13+0100`, then at `2026-06-23T16:12:03+0100` logs `summarise signal` in `current_phase=parse` with `records=0`, `elapsed=50s`, `heap_alloc_mb=8033`, `heap_sys_mb=8407`, `rss_mb=8567`, `goroutines=4`, `gc_count=104`, `mount_path="/warehouse/cellgeni/"`, `query_timeout=30s`, and `signal=SIGINT`.
  - Regression context: commits `0c5ba54` and `37447fc` routed large inputs directly to dirbuild and trimmed dirbuild's retained directory index, but a different production mount still exceeds the 8GB job limit. The next fix must introduce a genuinely bounded full-build path or an earlier safe guard for full production-scale directory cardinality, not just another small per-node optimization.
  - Investigation: the real production file copied to `/home/ubuntu/output/nfs/20260621-170004_／warehouse／cellgeni/stats.gz` is small by compressed size (34 MiB, 385 MiB uncompressed) but contains 1,905,655 rows, 207,968 dirs, max depth 18, and no file hardlinks. A lightweight contiguity probe classifies it as non-contiguous in about 4 MiB heap, so the old size-only probe threshold missed it and let it enter the heavier path late.
  - Root cause: for non-contiguous ClickHouse spool builds with basedirs enabled, dirbuild's basedirs adapter retained one per-directory summary node/map until after parse. The `cellgeni` file therefore kept summaries for hundreds of thousands of directories live at parse completion, producing RSS above the 8 GiB reservation even though configured basedirs only need output-base summaries.
  - Fix: every reopenable stats file is now probed cheaply before the fast path; non-contiguous files go directly to dirbuild. Dirbuild basedirs now accumulates each file directly into its configured base directory and immediate-child bucket instead of retaining a full per-directory basedirs tree. Full-filter child tracking was also bounded to the last child per tuple for dirbuild's sorted pre-order output.
  - Evidence: a bounded external probe of the real `cellgeni` file with basedirs enabled and no `GOMEMLIMIT` completed in 44s with `records=1905655`, `shape=non_contiguous`, `path=dirbuild`, `heap_alloc_mb=3683`, `heap_sys_mb=4751`, `rss_mb=4354`, and sampled peak heap-in-use growth 4742.8 MiB. Before direct basedirs accumulation, the same run reached `heap_alloc_mb=6676`, `heap_sys_mb=9523`, `rss_mb=9769`.

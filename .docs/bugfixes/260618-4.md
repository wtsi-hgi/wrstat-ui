- [x] The version before commit `17090b4` hit a Go runtime fatal while summarising `/nfs/wrstat/nfs/20260615-170002_／nfs／t283_imaging/stats.gz` for mount `/nfs/t283_imaging/`: `runtime: invalid pc-encoded table f=github.com/wtsi-hgi/wrstat-ui/cmd.summariseWriteNextMergedStatsSortChunk ... fatal error: invalid runtime symbol table` during SIGURG async preemption, after `summarise start` and before any parse progress log.
  - Root cause: `cmd/summarise_stats_sort.go` is byte-identical before and after `17090b4`, so the Unicode stats escape fix did not change the crashed merge path. The production `cmd` sort/merge code uses no unsafe/linkname/compiler directives, and the fatal occurred in goroutine 0 while the Go runtime decoded async-preemption PC data for a small valid Go wrapper around heap pop/write/advance. That points to Go runtime/compiler symbol metadata failure, or less likely deployed-binary corruption, rather than malformed stats input or an application data-structure panic. The exact production binary is unavailable, so binary corruption cannot be absolutely excluded; the in-repo mitigation removes the exact wrapper symbol and by-value record-copy site named by the runtime fatal.
  - Fix: folded `summariseWriteNextMergedStatsSortChunk` into `summariseWriteMergedStatsSortChunks` and changed the output helper to receive `*summariseStatsSortRecord`, preserving the existing external merge ordering, duplicate directory boundary suppression, synthetic parents, component preorder, Unicode escape fix, and schema repair while removing the fragile async-preemption target from the hot merge loop.
  - Regression coverage: direct reproduction is not deterministic because the failure is a fatal Go runtime metadata decode during async preemption and kills the process outside normal Go error handling. Added `TestSummariseStatsSortMergeHotPathSymbols`, which compiles a bounded temporary `./cmd` test binary and asserts that the exact crashed symbol `github.com/wtsi-hgi/wrstat-ui/cmd.summariseWriteNextMergedStatsSortChunk` is absent while still touching the merge path. The failing-first run failed with `Expected: false Actual: true`; after the fix it passes. Existing `TestSummariseStatsSort` coverage still verifies chunk merge ordering, duplicate directory boundaries, synthetic parents, component preorder, and escaped Unicode sibling ordering.
  - Gates: failing first run `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./cmd -run TestSummariseStatsSortMergeHotPathSymbols -v` failed with `Expected: false Actual: true`; `cleanorder -min-diff cmd/summarise_stats_sort.go cmd/summarise_stats_sort_test.go` passed; `timeout 300s golangci-lint run --fix` passed with `0 issues`; `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./cmd -run 'TestSummariseStatsSort|TestSummariseClickHouseSpoolRows' -v` passed; `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./clickhouse ./cmd ./internal/chperf -v` passed; `timeout 300s make lintnonpm` passed with `0 issues`; `timeout 120s git diff --check` passed; `timeout 900s make testnonpm` passed; `timeout 600s make buildnonpm` passed.

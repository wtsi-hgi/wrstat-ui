- [x] Add ClickHouse perf coverage that distinguishes cold provider, warmed provider, and post-provider-update query behaviour for web-facing Disktree and `where` operations, so cache warmup is measured explicitly instead of being mistaken for a fixed regression.
  - Added perf operations for cold-provider `where`, cold-provider Disktree endpoint, and provider-update cold-cache variants, all measured with wall time so provider/cache setup is visible where intended.
  - Added setup/teardown support in the perf harness so cold-cache and provider-update operations can reset caches and open/close providers per timed repeat.
  - Added a ClickHouse query-cache reset hook for the perf harness and regression coverage proving maintained projection readiness misses are retried while readiness hits remain cached.
  - Added file-level operation metadata for list-dir, stat-path, and glob cases so file APIs are clearly represented in perf reports.
  - Gates: `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./clickhouse ./cmd ./internal/chperf`; `timeout 600s make testnonpm`; `timeout 300s make lintnonpm`; `timeout 120s go mod tidy -diff`; `timeout 120s git diff --check`.
- [x] Restore and improve ClickHouse web-responsive query performance for Disktree-style tree interactions and `where`, including fresh-provider/cold-cache paths, without removing or weakening summarise projection correctness.
  - Added a maintained `wrstat_dir_summary.child_count` fast path for broad `DirsHaveChildren` checks so Disktree child-existence probes avoid querying raw `wrstat_children` when the maintained projection is ready.
  - Carried maintained child counts through the dir-summary query/cache layer and added cache consistency coverage so summary-only writes, eviction, and reset cannot leave stale child-count answers.
  - Added regression coverage for true, false, missing, and large-fanout child-existence checks, preserving fallback behaviour for unavailable maintained summaries.
  - Gates: `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./clickhouse ./cmd ./internal/chperf`; `timeout 600s make testnonpm`; `timeout 300s make lintnonpm`; `timeout 120s go mod tidy -diff`; `timeout 120s git diff --check`.
- [x] Add perf coverage and optimization targets for ClickHouse file-level APIs that Bolt never supported and the web UI/`where` do not currently exercise, including list-dir, stat-path, and glob/find queries.
  - Changed the ClickHouse perf adapter to request narrow file fields for perf-only file operations: `path`, `ext`, and `entry_type` for `ListDir`, and `path` for `StatPath` and `FindByGlob`.
  - Added adapter regression coverage proving chperf requests only the columns each file-level operation needs.
  - Added ClickHouse file API query-shape coverage proving explicit field lists produce narrow SELECTs while empty field lists retain the public full-row defaults.
  - Gates: `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./internal/chperf ./clickhouse -run 'TestBuildOps|TestClickHouseQueryClient|Test.*File|Test.*Glob|TestClient.*Path'`; `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./clickhouse ./cmd ./internal/chperf`; `timeout 600s make testnonpm`; `timeout 300s make lintnonpm`; `timeout 120s go mod tidy -diff`; `timeout 120s git diff --check`.
- [x] Add summarise/import perf guardrails that run alongside query perf so future query-speed fixes cannot break large summarise jobs, active-snapshot publishing, or maintained projection availability.
  - Added per-dataset `import_guardrail` report operations for raw file ingest, active snapshot publish, maintained dir projection, and active tree summary refresh.
  - Missing import guardrail phases are now explicit `status=missing` rows with zero duration, so perf reports show when publish/projection/tree-summary work was not observed instead of silently omitting it.
  - Added tests for observed guardrails, missing guardrails, and the stricter raw-ingest rule that parser lines alone do not prove `wrstat_files` ingest happened.
  - Gates: `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./internal/chperf -run 'TestImportReportOperations|Test.*Import.*Guardrail'`; `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./clickhouse ./cmd ./internal/chperf`; `timeout 600s make testnonpm`; `timeout 300s make lintnonpm`; `timeout 120s go mod tidy -diff`; `timeout 120s git diff --check`.
- [ ] Audit startup and cache warming behaviour after `server started`, document what work is synchronous vs background, and add tests or perf metadata that make cold/warm/provider-update timing visible.

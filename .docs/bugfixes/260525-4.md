- [x] ClickHouse active snapshot cleanup can still fail with `clickhouse: failed to clean active snapshot mount row: mount_path=... snapshot_id=... is still active`; cleanup must reliably make the failed deterministic snapshot inactive so `--clickhouse-recover` retries can proceed, even if older `wrstat_mounts` rows have been compacted away or the rollback metadata row does not win the active view immediately.
  - Added an `active` metadata flag to `wrstat_mounts` and replaced `wrstat_mounts_active` so the latest inactive mount row hides the mount from the active view.
  - Changed cleanup to insert inactive tombstone rows instead of relying on mount-row DELETE, including fallback to a tombstone if rollback to an older snapshot does not make the failed SID inactive.
  - Changed normal snapshot publishing to use a server-side monotonic `switched_at` based on the mount's existing max timestamp, so immediate recover retries outrank cleanup tombstones.
  - Added regression coverage for no-previous-snapshot cleanup, rollback fallback, active-view tombstone semantics, and immediate retry after tombstone cleanup.

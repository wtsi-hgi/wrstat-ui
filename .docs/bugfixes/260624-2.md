- [x] Still failing for some. `~/output/lustre/20260517-200015_／lustre／scratch120/stats.gz` has been updated to be the one that caused this error:
  `t=2026-06-24T11:48:11+0100 lvl=info msg="summarise start pid=3245751 input=\"/nfs/wrstat/lustre/20260623-200015_／lustre／scratch120/stats.gz\" output=\"/nfs/wrstat/clickhouse/watch_sb10/db/.20260623-200015_／lustre／scratch120\" current_phase=preflight mount_path=\"/lustre/scratch120/\" mountpoints=\"/nfs/wrstat/mounts\" snapshot_id=acf49b20-ba9e-5ab7-bfc8-f71b3ec9a159 updated_at=\"2026-06-23T20:51:50.529434Z\" clickhouse_database=\"prod\" clickhouse_dsn=\"clickhouse://wrstat:xxxxx@farm22-wrstat01:19001/default?compress=lz4&database=prod&dial_timeout=1s&secure=true&skip_verify=true\" query_timeout=30s signals=\"SIGTERM,SIGINT,SIGQUIT\" note=\"SIGKILL cannot be caught; use the last progress/RSS breadcrumb before an LSF kill\""`
  `t=2026-06-24T11:55:03+0100 lvl=info msg="summarise signal pid=3245751 input=\"/nfs/wrstat/lustre/20260623-200015_／lustre／scratch120/stats.gz\" output=\"/nfs/wrstat/clickhouse/watch_sb10/db/.20260623-200015_／lustre／scratch120\" records=0 elapsed=6m51s current_phase=parse heap_alloc_mb=7736 heap_sys_mb=8199 rss_mb=8318 goroutines=4 gc_count=415 recent_import_phases=\"\" total_import_phases=\"\" mount_path=\"/lustre/scratch120/\" mountpoints=\"/nfs/wrstat/mounts\" snapshot_id=acf49b20-ba9e-5ab7-bfc8-f71b3ec9a159 updated_at=\"2026-06-23T20:51:50.529434Z\" clickhouse_database=\"prod\" clickhouse_dsn=\"clickhouse://wrstat:xxxxx@farm22-wrstat01:19001/default?compress=lz4&database=prod&dial_timeout=1s&secure=true&skip_verify=true\" query_timeout=30s signal=SIGINT"`
  - Regression context: commits `24192d8` and `7ae2cfb` bounded dirbuild rollup retention and compacted GUTA age summaries, but this exact Lustre scratch file still dies in `current_phase=parse` after 6m51s with `records=0`, about 7.7 GiB heap allocated, and 8.3 GiB RSS. The local file is 1,407,657,133 compressed bytes, so the next fix must identify the remaining large-Lustre retained parse structure or produce evidence that this class needs a higher reservation.
  - Blocked after investigation on the exact local file. Shape scan found 87,962,401 rows, 12,916,741 directories, 75,045,660 files, 852,111 hardlinked file rows, max path length 939, and max depth 33.
  - The file probes as `contiguous=false` in about 130ms using around 2 MiB heap, so the fast path is not valid and dirbuild is required for correctness.
  - Current-code dirbuild phase probe retained about 5,449 MiB heap alloc / 6,945 MiB heap-in-use after the directory index, then about 15,155 MiB heap alloc / 17,107 MiB heap-in-use after pass 2. A narrow lazy GUTA age-cache experiment lowered pass-2 retained heap to about 12,265 MiB, but peak RSS still reached about 22 GiB, so the partial code was reverted.
  - Conclusion: this exact high-directory-count non-contiguous Lustre class does not fit the current 8 GiB in-memory dirbuild design. A 16 GiB reservation is also risky for this class because the bounded probe exceeded it.
  - Next viable options are: implement a disk-backed or streaming dirbuild summary store keyed by directory id; temporarily reserve about 24-32 GiB for this extreme class; or reintroduce an external-sort fallback only for extreme high-directory-count mounts, accepting the write amplification as an explicit tradeoff.
  - Implemented the hybrid option generically rather than hardcoding `scratch120`: ordinary mounts keep the in-memory dirbuild path, while high-directory-count shapes route to a disk-backed summary store before pass 2 summary accumulation starts.
  - Compacted the extreme-path topology by cloning retained path components, avoiding long raw-path retention, removing permanent per-parent child-name slices, and materializing child names only at emit time. The routing remains shape-based and does not retry after an in-memory blow-up.
  - Added disk-backed dirbuild summaries under the controlled spool `.partial` directory. The SQLite scratch is included in `build_phase_bytes_written` and removed before the final spool is published, so A5/reporting still sees the write cost.
  - Added behavioural coverage for disk-backed equivalence with in-memory dirbuild on representative non-contiguous data, file rows, hardlinks, full-filter child counts, topology memory, forced disk-backed report accounting/cleanup, and the small fast path.
  - Exact-file bounded evidence on the updated `scratch120` file reached disk-backed summary scratch creation up front. RSS at the disk-summary transition was about 4,883,784 KB and `/usr/bin/time` max RSS was 5,546,928 KB before interruption at 4m42s, compared with the earlier 8,830,716 KB bounded topology/index peak and 17,107 MiB heap-in-use after pass 2.
  - Implementor and reviewer agents ran focused `summary/dirbuild`, `cmd`, and `internal/chperf` tests, `golangci-lint`, `git diff --check`, `make lintnonpm`, and `make testnonpm`. Full 88M-row completion was not rerun in review, so production should still watch runtime and disk growth for this first extreme-class run.

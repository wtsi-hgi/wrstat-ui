- [x] The previous startup fix only stopped a slow ClickHouse tree-summary backfill from aborting server startup; if the backfill exceeded the normal query timeout, the derived fast-path summaries could be left unbuilt forever. Fix this properly so the backfill either waits without the normal query timeout during startup or runs asynchronously after startup, and logs when it completes.
  - Added provider-owned ClickHouse tree-summary maintenance refresh that is scheduled after inline timeout, runs without the normal query timeout, dedupes by active fingerprint, and is cancelled/waited by provider close.
  - Marked the already-published active snapshot summary-ready on successful async refresh so the Disktree fast path becomes available without server restart.
  - Added provider message callbacks and server logging for successful refresh completion, while refresh failures continue through `OnError`.
  - Added regressions for async refresh after startup timeout, completion logging, and server/provider callback wiring.

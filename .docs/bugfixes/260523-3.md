- [x] Starting a server against an existing production ClickHouse database can fail during provider open because automatic tree-summary backfill runs synchronously inside the normal query timeout: `failed to refresh active tree summaries: failed to insert tree dguta summary: context deadline exceeded`. Startup must not fail just because the derived ancestor summary backfill is slow; it should either use an appropriate maintenance path or fall back and complete/refresh safely without breaking existing endpoints.
  - Made startup and DGUTA snapshot tree-summary refresh best-effort: active readers are still published, writer close still succeeds after switching the active snapshot, and ancestor queries fall back until maintained summaries are available.
  - Kept refresh failures visible through the provider error callback, including failures queued before polling starts and before `OnError` is registered.
  - Added ClickHouse regressions for startup refresh timeout fallback, delayed `OnError` delivery, request-time fallback while summaries are unavailable, and DGUTA writer close behavior.

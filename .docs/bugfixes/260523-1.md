- [x] ClickHouse-backed Disktree `/` and ancestor navigation still reconstruct virtual ancestor summaries (`/`, `/lustre`, `/nfs`, etc.) by fetching many DGUTA rows across active mounts and summing them in Go on every request, so page refresh and repeated root/ancestor clicks stay slow. Add storage/query support for direct, automatically maintained ancestor summary lookups and direct child metadata such as filter-aware `has_children` where appropriate, update perf subcommands to prove the Disktree `/` and click-through paths, and verify on sizable Lustre+NFS subsets from `~/output/lustre` and `~/output/nfs`.
  - Added maintained ClickHouse tree-summary tables keyed by active mount fingerprint, with compact age-only directory summaries, filter-preserving GUTA summaries, and ancestor child metadata.
  - Refreshed/backfilled tree summaries automatically on provider open and DGUTA snapshot switch so active mount additions and updates publish new summary fingerprints without manual SQL.
  - Routed virtual ancestor `DirInfo`, `Children`, and `DirsHaveChildren` through the derived tables while preserving nil-versus-empty UID/GID filter semantics and group/user/type/age filtering.
  - Added `tree_disktree_endpoint_ancestor_dirs` perf coverage with `--ancestor-dir` / `--ancestor-limit` for ClickHouse and Bolt perf paths.
  - Verified on the combined local Lustre+NFS benchmark that repeated `/` Disktree improved from the earlier ~109 ms warm profile to 14.7 ms p50 / 24.9 ms p95, with mixed ancestor click-through at 16.3 ms p50.

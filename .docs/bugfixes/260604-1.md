- [x] Despite `.docs/bugfixes/260603-1.md`, `t283_imaging` is still failing to summarise. Recent failed `wr` attempts mostly show "lost contact with runner" likely from crash/kill before the runner can report. Recent log tails: at 320,000,000 records and 4h25m in `current_phase=wrstat_children_insert` with `heap_alloc_mb=33523`, `heap_sys_mb=38850`, `rss_mb=35872`; another attempt failed flushing basedirs group usage batches with context deadline exceeded; another at 320,000,000 records and 5h54m in `wrstat_children_insert` with `heap_alloc_mb=35928`, `heap_sys_mb=38954`, `rss_mb=39314`. The fix must consider whether `t283_imaging` has something bad beyond "more directories" deeper in the dataset, and must have no regressions to summarise/query speed or significant summarise memory increase, proven with current and after `clickhouse-perf` measurements.
  - Root cause: `summary/dirguta` retained every direct child name in each active directory until that directory output. A deep t283 directory with huge sibling fanout could therefore keep millions of child strings alive; the diagnostic `wrstat_children_insert` phase was the last recorded child-write phase, not necessarily the only retained state.
  - Approach: added optional `db.DGUTAChildrenWriter` streaming, explicit `RecordDGUTA.ChildCount`, bounded child-name chunks (16,384 names), ClickHouse `AddChildren`, and chperf row tracking for streamed child rows. Non-streaming DGUTA sinks keep the previous `Children` behaviour.
  - Files touched: `db/dguta.go`, `summary/dirguta/dirguta.go`, `summary/dirguta/dirguta_test.go`, `clickhouse/dguta_writer.go`, `clickhouse/dguta_writer_test.go`, `internal/chperf/import.go`, `internal/chperf/import_test.go`.
  - Coverage: added a 50,000-child fanout regression proving the large record retains zero child names while streaming all child rows, a ClickHouse integration test for streamed children plus final child count, and a chperf metric test for streamed child row accounting.
  - Perf proof saved under `.tmp/agent/260604-1-baseline/` and `.tmp/agent/260604-1-after/`: t283 2M import rows unchanged (`wrstat_files=2,000,000`, `wrstat_children=818,478`, `wrstat_dir_facts=1,638,473`), 66.673s/707.6 MiB baseline vs 68.786s/712.5 MiB after; scratch127 2M rows unchanged (`wrstat_children=52,124`, `wrstat_dir_facts=132,520` plus basedirs rows), 14.994s/673.7 MiB baseline vs 15.457s/651.5 MiB after. Query timings stayed in the same low-ms band: t283 tree/new-dirs 8.28ms vs 9.40ms, listdir p50/p95 5/5ms vs 5/6ms, glob p50/p95 5/5-6ms vs 5-6/6ms; scratch127 tree/new-dirs 8.88ms vs 9.72ms, listdir 5/5ms both, glob p50/p95 5/5-6ms vs 5-6/5-6ms.
  - Gates: `cleanorder -min-diff ...`; `gofmt -w ...`; `timeout 300s golangci-lint run --fix`; focused `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./summary/dirguta -run TestDirGUTA -v`, `./clickhouse -run TestClickHouseDGUTAWriter -v`, `./internal/chperf -run TestDGUTARowCounting -v`; `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./clickhouse ./cmd ./internal/chperf`; `timeout 600s make testnonpm`; `timeout 300s make lintnonpm`; `timeout 120s go mod tidy -diff`; `timeout 120s git diff --check`.

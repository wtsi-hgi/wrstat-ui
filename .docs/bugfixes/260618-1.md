- [x] Latest code and compiled executable still fail some summarise jobs with `non-contiguous directory input` after previous unordered-stats fixes. Production example: `/nfs/wrstat/nfs/20260616-170003_／nfs／cancer4/stats.gz` for mount `/nfs/cancer4/` fails during parse after 13s on 2026-06-18 with `current_phase=parse_failed`, heap_sys around 503 MB, and `err="non-contiguous directory input"`.
  - Root cause: the sorted retry still keyed parsed stats records by the unquoted full path string. That is not the same as the summariser/allocator directory preorder, which compares path components without their slash terminators. A sibling such as `project.v2/` sorts before `project/` in raw string order because `.` sorts before `/`, so the retry could close the `project.v2/` subtree and then later try to enter the earlier `project/` sibling, triggering `ErrNonContiguousInput`.
  - Fix: parsed retry records now use a component-preorder sort key built from the parser-unquoted path components, with the same key used for explicit directory rows, synthetic parent directory rows, and file rows. The output-local chunking, external merge, duplicate directory suppression, missing-parent synthesis, and completed-spool retry/publish behavior are unchanged; the allocator still produces real `dir_id` / `subtree_end` intervals.
  - Regression coverage: added a sorter regression proving `/mnt/test/project/` is emitted before `/mnt/test/project.v2/` even when the raw input arrives in the opposite full-string order; added a command/spool regression whose current-HEAD first run failed with `non-contiguous directory input`, then verifies valid catalog intervals and correct file-to-directory `dir_id` references for the fixed stream.
  - Gates: failing first run `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./cmd -run 'TestSummariseStatsSort|TestSummariseClickHouseSpoolRows' -v` failed with `non-contiguous directory input`; `cleanorder -min-diff cmd/summarise_stats_sort.go cmd/summarise_stats_sort_test.go cmd/summarise_spool_test.go`; `timeout 300s golangci-lint run --fix`; `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./cmd -run 'TestSummariseStatsSort|TestSummariseClickHouseSpoolRows' -v`; `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./clickhouse ./cmd ./internal/chperf -v`; `timeout 300s make lintnonpm`; `timeout 120s git diff --check`; `timeout 900s make testnonpm` passed.

- [x] `wrstat-ui watch` retries still cannot recover automatically after a failed ClickHouse `summarise` attempt leaves the same deterministic snapshot active without a completion marker: the retry should clean only that failed snapshot's active pointer and per-snapshot partitions, then reprocess from scratch, while preserving any prior successful snapshot for the same mount.
  - Added ClickHouse cleanup for a failed active snapshot attempt, deleting only matching `wrstat_mounts` rows and per-snapshot partitions.
  - Updated watch retry preflight so absent or mismatched completion markers clean the failed attempt and continue normal summarise processing, while matching markers still short-circuit as complete.
  - Added regressions for retry cleanup and reprocess, matching-marker no-op, manual refusal, older snapshot preservation, and basedirs history preservation.

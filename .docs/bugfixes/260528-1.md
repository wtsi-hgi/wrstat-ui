- [x] `t283_imaging` now gets past DGUTA import but fails in `wrstat_dir_projection_insert` after about 10 minutes with `clickhouse: failed to flush dir summary batch ... block_rows=10000 ... connection reset by peer`; make directory projection summary/vector imports robust by avoiding fragile long-lived flushed projection batches while preserving bounded memory, final close semantics, and the existing DGUTA send-per-block fix.
  - Changed full `wrstat_dir_summary` and `wrstat_dir_dguta_vector` projection batches to `Send()` discrete capped blocks and prepare fresh replacement batches, matching the raw DGUTA send-per-block pattern.
  - Preserved final partial projection batch close semantics through the existing close-time `Send()` path.
  - Added regression coverage for projection send/reprepare behavior, projection reprepare failure safety, and final partial projection batch sends.
  - Proved the fix on a bounded 2M-line local `t283_imaging` import: 2,000,000 records, 18,044,757 DGUTA rows, 11,768,795 dir summary rows, and 818,479 dir vector rows imported successfully in 9m30s with max RSS about 823 MiB.
  - Gates: `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./clickhouse ./cmd ./internal/chperf`; `timeout 600s make testnonpm`; `timeout 300s make lintnonpm`; `timeout 120s go mod tidy -diff`; `timeout 120s git diff --check`.

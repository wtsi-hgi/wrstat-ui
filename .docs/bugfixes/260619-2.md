- [x] scratch122 failed again on the latest version. The attached log shows `/nfs/wrstat/lustre/20260617-200026_／lustre／scratch122/stats.gz` on mount `/lustre/scratch122/` parsing to 14,000,000 records on 2026-06-19, then failing in `current_phase=parse_failed` with `err="non-contiguous directory input"` even after commit `508f9bf` fixed same-name file/directory retry ordering. The local replacement file remains `/home/ubuntu/output/lustre/20260517-200015_／lustre／scratch122/stats.gz`.
  - Root cause: the real scratch122 input contains explicit `d` directory rows without trailing slashes, including raw line 14,956,960 ending in `bins/maxbin2.006`. The retry sorter emitted the malformed explicit row unchanged and suppressed the normalized synthetic boundary, then the summariser saw a slashless directory boundary and reported `non-contiguous directory input`.
  - Fix: explicit directory rows in the retry sorter now key and emit slash-normalized paths only when the parsed `d` row is missing its trailing slash; already well-formed directory rows, files, synthetic parents, duplicate-boundary suppression, component ordering, Unicode escape behavior, same-name file/directory ordering, and schema repair are preserved.
  - Regression coverage: added sorter-level normalized-output coverage and summarise/spool coverage through the real command path, spool retry, stats parser, summariser, and dir-id allocator.
  - Gates: focused `./stats ./cmd` regressions, ClickHouse package tests, `make lintnonpm`, `git diff --check`, `make testnonpm`, and `make buildnonpm` all passed. The reviewer also spot-checked the real raw scratch122 line.

- [x] wrstat-ui clickhouse-perf import /home/ubuntu/output/wrstat/final --owners $OUTDIR/owners.csv --quota $OUTDIR/quota.csv --config $OUTDIR/basedirs.config --json $OUTDIR/json.clickhouse fails with Error: clickhouse: failed to append basedirs group subdir: clickhouse: batch has already been sent
  - Touched `clickhouse/basedirs_store.go` so subdir append loops use the store batch slot after a mid-call flush reprepares it.
  - Added a regression in `clickhouse/basedirs_store_test.go` covering both group and user subdir multi-row calls across a batch boundary.

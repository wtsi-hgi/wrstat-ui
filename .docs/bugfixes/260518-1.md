- [x] `wrstat-ui clickhouse-perf query` after importing root data auto-selects `dir=/`, prints `directory is empty, skipping StatPath`, and may therefore make the query timings meaningless.
  - Touched `clickhouse/file_ingest_operation.go` so root-mount stats paths like `//boot/` are imported as canonical `/boot/` rows, directory entries are stored under their parent, and the synthetic root self-row is skipped.
  - Touched `clickhouse/dguta_writer.go` so root-mount DGUTA and children rows are canonicalised consistently without double-counting `/` and `//` alias rows.
  - Touched `clickhouse/database.go` so inactive nested local mountpoints do not shadow an active imported `/` snapshot during tree queries.
  - Added regressions in `clickhouse/file_ingest_operation_test.go` and `clickhouse/dguta_writer_test.go` proving `ListDir("/")`, `DirInfo("/")`, and `Children("/")` work after root-mount import.
  - Added a regression in `clickhouse/database_dirinfo_test.go` proving `Tree().DirInfo("/")` can traverse children like `/dev` when only the imported `/` snapshot is active.
- [x] `wrstat-ui clickhouse-perf query` reports glob cases B, C, and D as much too slow after importing the ClickHouse dataset.
  - Touched `clickhouse/file_api.go` so direct-child globs use `parent_dir` equality plus `name` matching, and whole-subtree globs avoid redundant path regex checks.
  - Added query-shape regressions in `clickhouse/file_api_test.go` covering the direct and recursive glob SQL produced for the perf cases.

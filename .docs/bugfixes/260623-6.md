- [x] It's still not fixed: production `summarise` for `/nfs/wrstat/lustre/20260622-200025_／lustre／scratch120/stats.gz` starts at `2026-06-23T17:50:11+0100`, then at `2026-06-23T17:51:30+0100` logs `summarise signal` in `current_phase=parse` with `records=0`, `elapsed=1m19s`, `heap_alloc_mb=8220`, `heap_sys_mb=9051`, `rss_mb=9304`, `goroutines=5`, `gc_count=52`, `mount_path="/lustre/scratch120/"`, `query_timeout=30s`, and `signal=SIGINT`. In the same run, production `summarise` for `/nfs/wrstat/nfs/20260622-170004_／nfs／cancer_ref01/stats.gz` starts at `2026-06-23T17:50:24+0100`, then at `2026-06-23T17:51:45+0100` logs `summarise signal` in `current_phase=parse` with `records=0`, `elapsed=1m20s`, `heap_alloc_mb=8816`, `heap_sys_mb=9023`, `rss_mb=9089`, `goroutines=4`, `gc_count=100`, `mount_path="/nfs/cancer_ref01/"`, `query_timeout=30s`, and `signal=SIGINT`.
  - Regression context: commits `0c5ba54`, `37447fc`, and `8dd7ba2` fixed the size-threshold miss, trimmed dirbuild index memory, and bounded non-contiguous basedirs aggregation, but large production mounts still exceed the 8GB reservation. The next fix must identify the remaining retained parse structure on these mounts and either bound it or explicitly prove that the minimum reservation must be higher for this class.
  - Fixed the remaining retention in `summary/dirbuild`: large spools now route to dirbuild at 32 MiB, dirbuild drops the path index before rollup, restores GC pacing after the build, and emits each directory as its rollup is materialized instead of retaining every directory's GUTA rows until a final emit pass.
  - Preserved schema3 full-filter correctness for child-before-parent dirbuild emission by accumulating future direct-child tuple counts until each parent row is emitted, with a forced-dirbuild nested regression test for `FilterChildCount` and `HasFilterChildren`.
  - Kept production evidence accurate by reporting size-based dirbuild routing as `input_shape=large_unprobed`, while probe/retry dirbuild still reports `non_contiguous`, and updated A5 build gates to accept the new bounded large-file evidence without accepting healthy `non_contiguous` reports.
  - Investigation on the exact local `cancer_ref01` file showed 53,921,385 compressed bytes, 4,925,892 rows, 246,228 directories, and 4,679,664 files; the prior dirbuild memory probe peaked at about 3.88 GiB heap-in-use and retained about 28.8 MiB after build. The exact failing `scratch120` production file was not local, so it is covered by the shared large-spool dirbuild path and tests.
  - Implementor and reviewer agents ran focused `cmd`, `summary/dirbuild`, and `internal/chperf` tests, `golangci-lint`, `git diff --check`, and `make testnonpm`.

- [x] Summarising t283_imaging (see inside `~/output/nfs/`) has been running for over 16hrs in production now and still hasn't finished. At least it hasn't crashed yet. But this is too slow. The aim is for 12hrs max. Implement speed improvements for it via the bugfix skill workflow without introducing new crashes and without regressing any other performance aspects (including query times).
  - Found shared import hot-path allocation waste in raw DGUTA duplicate tracking and maintained dir projection vector construction, both amplified by t283-style directory-heavy imports.
  - Changed non-root DGUTA imports to track record ages with a bitset and avoid retaining full duplicate keys unless root-mount canonicalisation needs them.
  - Removed projection GUTA deep-cloning, avoided escaping age-bucket array copies, and made projection vector sorting allocation-free while preserving the same row schema and sorted vector order.
  - Added regression coverage proving non-root imports do not retain the heavy duplicate-key map and projection vector column construction stays below the allocation guardrail.
  - Gates:
    - `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./clickhouse -run 'TestDGUTAWriter|TestMountDirProjection|TestClickHouseImportBatchContexts|Test.*Batch|Test.*Projection|Test.*Receive|Test.*Import|Test.*Perf'`
    - `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./internal/chperf -run 'TestImportReportOperations|Test.*Import.*Guardrail|TestBuildOps|TestStartupCacheWarmingAudit|Test.*Query|Test.*File'`
    - `timeout 600s env CGO_ENABLED=1 go test -tags netgo --count 1 ./clickhouse ./cmd ./internal/chperf`
    - `timeout 600s make testnonpm`
    - `timeout 300s make lintnonpm`
    - `timeout 120s go mod tidy -diff`
    - `timeout 120s git diff --check`
